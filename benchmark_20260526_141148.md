# Genie Benchmark Results — benchmark_20260526_141148.jsonl

## Overall

- Total questions: **259**
- GOOD: **139** (53.7%)
- NEEDS_REVIEW: **1** (0.4%)
- BAD: **119** (45.9%)

## By subindustry

| Subindustry | Total | GOOD | NEEDS_REVIEW | BAD | Pass% |
|---|---:|---:|---:|---:|---:|
| logistics | 21 | 13 | 0 | 8 | 61.9% |
| machinery | 77 | 45 | 1 | 31 | 58.4% |
| oil_gas_integrated | 42 | 24 | 0 | 18 | 57.1% |
| oil_gas_midstream | 56 | 26 | 0 | 30 | 46.4% |
| oil_gas_refining | 42 | 22 | 0 | 20 | 52.4% |
| oil_gas_upstream | 21 | 9 | 0 | 12 | 42.9% |

## By spec

| Subindustry | Use case | Total | GOOD | NEEDS_REVIEW | BAD | Pass% |
|---|---|---:|---:|---:|---:|---:|
| logistics | fleet_planning_and_optimization | 7 | 6 | 0 | 1 | 85.7% |
| logistics | load_demand_forecasting | 7 | 4 | 0 | 3 | 57.1% |
| logistics | route_planning | 7 | 3 | 0 | 4 | 42.9% |
| machinery | asset_health | 7 | 3 | 0 | 4 | 42.9% |
| machinery | demand_forecasting | 7 | 4 | 0 | 3 | 57.1% |
| machinery | field_service_assistant | 7 | 6 | 0 | 1 | 85.7% |
| machinery | financial_analytics_reporting | 7 | 2 | 0 | 5 | 28.6% |
| machinery | machining_process_defect_detection | 7 | 5 | 0 | 2 | 71.4% |
| machinery | manufacturing_resource_planning | 7 | 4 | 0 | 3 | 57.1% |
| machinery | production_monitoring | 7 | 5 | 0 | 2 | 71.4% |
| machinery | quality_event_root_cause_analysis | 7 | 6 | 0 | 1 | 85.7% |
| machinery | spare_part_inventory_optimization | 7 | 2 | 1 | 4 | 28.6% |
| machinery | spend_intelligence | 7 | 5 | 0 | 2 | 71.4% |
| machinery | working_capital_cash_flow_optimization | 7 | 3 | 0 | 4 | 42.9% |
| oil_gas_integrated | capital_investment_simulation | 7 | 4 | 0 | 3 | 57.1% |
| oil_gas_integrated | financial_analytics_reporting | 7 | 5 | 0 | 2 | 71.4% |
| oil_gas_integrated | predictive_maintenance_asset_health | 7 | 6 | 0 | 1 | 85.7% |
| oil_gas_integrated | production_monitoring_control_center | 7 | 2 | 0 | 5 | 28.6% |
| oil_gas_integrated | scenario_planning_business_simulation | 7 | 2 | 0 | 5 | 28.6% |
| oil_gas_integrated | working_capital_cash_flow_optimization | 7 | 5 | 0 | 2 | 71.4% |
| oil_gas_midstream | automated_reporting_of_carbon_intensity | 7 | 3 | 0 | 4 | 42.9% |
| oil_gas_midstream | energy_trading | 7 | 3 | 0 | 4 | 42.9% |
| oil_gas_midstream | financial_analytics_reporting | 7 | 4 | 0 | 3 | 57.1% |
| oil_gas_midstream | logistics_optimization | 7 | 2 | 0 | 5 | 28.6% |
| oil_gas_midstream | regulation_compliance | 7 | 4 | 0 | 3 | 57.1% |
| oil_gas_midstream | scenario_planning_business_simulation | 7 | 4 | 0 | 3 | 57.1% |
| oil_gas_midstream | spend_intelligence | 7 | 4 | 0 | 3 | 57.1% |
| oil_gas_midstream | working_capital_cash_flow_optimization | 7 | 2 | 0 | 5 | 28.6% |
| oil_gas_refining | energy_use_monitoring_heat | 7 | 5 | 0 | 2 | 71.4% |
| oil_gas_refining | financial_analytics_reporting | 7 | 5 | 0 | 2 | 71.4% |
| oil_gas_refining | predictive_maintenance_asset_health | 7 | 4 | 0 | 3 | 57.1% |
| oil_gas_refining | production_monitoring | 7 | 3 | 0 | 4 | 42.9% |
| oil_gas_refining | quality_event_root_cause_analysis | 7 | 3 | 0 | 4 | 42.9% |
| oil_gas_refining | working_capital_cash_flow_optimization | 7 | 2 | 0 | 5 | 28.6% |
| oil_gas_upstream | predictive_maintenance_asset_health | 7 | 2 | 0 | 5 | 28.6% |
| oil_gas_upstream | reservoir_management | 7 | 4 | 0 | 3 | 57.1% |
| oil_gas_upstream | well_production_monitoring_flow | 7 | 3 | 0 | 4 | 42.9% |

## Failing / needs-review questions (120)

### logistics/route_planning — BAD

**Q:** Which vehicles have the highest average on-time delivery percentage?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT vehicle_id, MEASURE(avg_on_time_delivery_pct) AS average_on_time_delivery_pct
FROM logistics_demos_catalog.route_planning.route_efficiency_metrics_mv
WHERE vehicle_id IS NOT NULL
GROUP BY ALL
ORDER BY average_on_time_delivery_pct DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `vehicle_id`, MEASURE(`avg_on_time_delivery_pct`) AS `average_on_time_delivery_pct` FROM `logistics_demos_catalog`.`route_planning`.`route_efficiency_metrics_mv` WHERE `vehicle_id` IS NOT NULL GROUP BY ALL HAVING `average_on_time_delivery_pct` IS NOT NULL ORDER BY `average_on_time_delivery_pct` DESC
```

### logistics/route_planning — BAD

**Q:** Which vehicles have the best load weight of vehicle capacity?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT vehicle_name, AVG(load_utilization_pct) AS avg_load_utilization_pct
FROM logistics_demos_catalog.route_planning.route_segments
GROUP BY vehicle_name
ORDER BY avg_load_utilization_pct DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_vehicles AS (
  SELECT 
    `vehicle_type`,
    MEASURE(`avg_load_utilization_pct`) AS `average_load_utilization_pct`,
    RANK() OVER (ORDER BY MEASURE(`avg_load_utilization_pct`) DESC) AS `utilization_rank`
  FROM `logistics_demos_catalog`.`route_planning`.`route_segments_metrics`
  WHERE `vehicle_type` IS NOT NULL
  GROUP BY ALL
)
SELECT 
  `vehicle_type`,
  `average_load_utilization_pct`
FROM ranked_vehicles
WHERE `utilization_rank` <= 10
ORDER BY `utilization_rank`
```

### logistics/route_planning — BAD

**Q:** Which vehicle class: semi, truck, or vans have the highest total fuel efficiency in kilometers per liter?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCORRECT_METRIC_CALCULATION

**Expected SQL:**
```sql
SELECT vehicle_type, SUM(fuel_efficiency_km_per_l) AS total_metric
FROM logistics_demos_catalog.route_planning.vehicles
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH efficiency_by_type AS (
  SELECT 
    `vehicle_type`,
    try_divide(MEASURE(`total_distance_km`),NULLIF(MEASURE(`total_fuel_consumed_liters`), 0)) AS fuel_efficiency_km_per_l
  FROM logistics_demos_catalog.route_planning.route_segments_metrics
  WHERE `vehicle_type` IS NOT NULL
  GROUP BY ALL
)
SELECT 
  `vehicle_type`,
  fuel_efficiency_km_per_l
FROM (
  SELECT *, RANK() OVER (ORDER BY fuel_efficiency_km_per_l DESC) as rnk
  FROM efficiency_by_type
) ranked
WHERE rnk = 1
```

### logistics/route_planning — BAD

**Q:** Which vehicles have the highest total weekly distance traveled?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
WITH vehicle_distances AS (SELECT vehicle_id, MEASURE(total_weekly_distance_km) AS total_weekly_distance_km
FROM logistics_demos_catalog.route_planning.route_efficiency_metrics_mv
GROUP BY ALL), ranked_vehicles AS (SELECT vd.vehicle_id, v.vehicle_name, vd.total_weekly_distance_km, RANK() OVER (ORDER BY vd.total_weekly_distance_km DESC) AS rnk
FROM vehicle_distances vd
LEFT JOIN logistics_demos_catalog.route_planning.vehicles v ON vd.vehicle_id = v.vehicle_id) SELECT vehicle_id, vehicle_name, total_weekly_distance_km
FROM ranked_vehicles
WHERE rnk <= 10
ORDER BY total_weekly_distance_km DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH vehicle_distances AS (
  SELECT 
    `vehicle_id`,
    `vehicle_type`,
    MEASURE(`total_weekly_distance_km`) AS total_distance
  FROM logistics_demos_catalog.route_planning.route_efficiency_metrics_mv
  GROUP BY ALL
)
SELECT 
  `vehicle_id`,
  `vehicle_type`,
  total_distance,
  RANK() OVER (ORDER BY total_distance DESC) AS distance_rank
FROM vehicle_distances
WHERE total_distance IS NOT NULL
QUALIFY distance_rank <= 10
ORDER BY distance_rank, vehicle_id
```

### machinery/quality_event_root_cause_analysis — BAD

**Q:** Which product line identifiers have the highest average defect rate in ppm?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT product_line, AVG(defect_ppm) AS avg_defect_ppm, RANK() OVER (ORDER BY AVG(defect_ppm) DESC) AS rank_defect_ppm
FROM logistics_demos_catalog.mach_quality_rca.quality_kpi_monthly
WHERE product_line IS NOT NULL AND defect_ppm IS NOT NULL
GROUP BY product_line
ORDER BY avg_defect_ppm DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_product_lines AS (
  SELECT 
    `product_line`, 
    AVG(`defect_ppm`) AS `average_defect_ppm`,
    RANK() OVER (ORDER BY AVG(`defect_ppm`) DESC) AS rnk
  FROM `logistics_demos_catalog`.`mach_quality_rca`.`quality_kpi_monthly`
  WHERE `product_line` IS NOT NULL AND `defect_ppm` IS NOT NULL
  GROUP BY `product_line`
)
SELECT `product_line`, `average_defect_ppm`
FROM ranked_product_lines
WHERE rnk <= 10
ORDER BY `average_defect_ppm` DESC
```

### machinery/field_service_assistant — BAD

**Q:** What are the top equipment model at customer site by total time to resolution in hours?

**Reasons:**
- EMPTY_RESULT

**Expected SQL:**
```sql
SELECT equipment_model, SUM(resolution_hours) AS total_resolution_hours
FROM logistics_demos_catalog.mach_field_service.service_tickets
GROUP BY equipment_model ORDER BY total_resolution_hours DESC LIMIT 10
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_models AS (
  SELECT
    `equipment_model`,
    SUM(`resolution_hours`) AS `total_resolution_hours`,
    RANK() OVER (ORDER BY SUM(`resolution_hours`) DESC) AS `resolution_rank`
  FROM `logistics_demos_catalog`.`mach_field_service`.`service_tickets`
  WHERE `equipment_model` IS NOT NULL AND `resolution_hours` IS NOT NULL
  GROUP BY `equipment_model`
)
SELECT `equipment_model`, `total_resolution_hours`
FROM ranked_models
WHERE `resolution_rank` <= 10
ORDER BY `resolution_rank`
```

### machinery/machining_process_defect_detection — BAD

**Q:** Which cnc machine models have the highest average defect rate percentage?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT machine_model, MEASURE(avg_defect_rate) AS avg_defect_rate
FROM logistics_demos_catalog.mach_machining_defect.defect_kpi_metrics
GROUP BY ALL
ORDER BY avg_defect_rate DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_models AS (
  SELECT 
    `machine_model`, 
    MEASURE(`avg_defect_rate`) AS `avg_defect_rate`,
    RANK() OVER (ORDER BY MEASURE(`avg_defect_rate`) DESC) AS defect_rate_rank
  FROM logistics_demos_catalog.mach_machining_defect.defect_kpi_metrics
  WHERE `machine_model` IS NOT NULL
  GROUP BY ALL
)
SELECT `machine_model`, `avg_defect_rate`
FROM ranked_models
WHERE defect_rate_rank = 1;
```

### machinery/machining_process_defect_detection — BAD

**Q:** Which cnc machine models have the highest total surface roughness ra in micrometers?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT machine_model, SUM(surface_finish_um) AS total_surface_finish_um
FROM logistics_demos_catalog.mach_machining_defect.machining_events
GROUP BY machine_model
ORDER BY total_surface_finish_um DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `machine_model`, MEASURE(`avg_surface_finish`) AS `total_surface_roughness_ra_um` FROM logistics_demos_catalog.mach_machining_defect.machining_events_metrics WHERE `machine_model` IS NOT NULL GROUP BY ALL ORDER BY `total_surface_roughness_ra_um` DESC LIMIT 10
```

### machinery/production_monitoring — BAD

**Q:** How has highest availability percent changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT oee_month, MEASURE(max_availability_percent) AS max_availability_percent
FROM logistics_demos_catalog.mach_production_mon.oee_metrics
GROUP BY ALL
ORDER BY oee_month
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `oee_month`, MEASURE(`max_availability_percent`) AS `max_availability_percent`
FROM logistics_demos_catalog.mach_production_mon.oee_metrics
GROUP BY ALL
ORDER BY `oee_month`
```

### machinery/production_monitoring — BAD

**Q:** How has highest planned qty changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT run_date, MEASURE(max_planned_qty) AS max_planned_qty
FROM logistics_demos_catalog.mach_production_mon.production_runs_metrics
GROUP BY ALL
ORDER BY run_date
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `run_date`, MEASURE(`max_planned_qty`) AS `max_planned_qty`
FROM logistics_demos_catalog.mach_production_mon.production_runs_metrics
GROUP BY ALL
ORDER BY `run_date`
```

### logistics/fleet_planning_and_optimization — BAD

**Q:** Which vehicles have the best dispatched hours of available hours?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT vehicle_name, AVG(utilization_pct) AS avg_utilization_pct
FROM logistics_demos_catalog.fleet_planning.vehicle_utilization_snapshots
GROUP BY vehicle_name
ORDER BY avg_utilization_pct DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH vehicle_utilization AS (
  SELECT 
    `vehicle_id`,
    `vehicle_name`,
    AVG(`utilization_pct`) AS avg_utilization_pct
  FROM `logistics_demos_catalog`.`fleet_planning`.`vehicle_utilization_snapshots`
  WHERE `vehicle_id` IS NOT NULL AND `utilization_pct` IS NOT NULL
  GROUP BY `vehicle_id`, `vehicle_name`
)
SELECT 
  `vehicle_id`,
  `vehicle_name`,
  avg_utilization_pct,
  RANK() OVER (ORDER BY avg_utilization_pct DESC) AS utilization_rank
FROM vehicle_utilization
ORDER BY utilization_rank ASC, `vehicle_id`
```

### machinery/asset_health — BAD

**Q:** Which type of industrial assets have the highest average asset availability percentage?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT asset_type, MEASURE(avg_availability) AS avg_availability
FROM logistics_demos_catalog.mach_asset_health.asset_health_monthly_metrics
GROUP BY ALL
ORDER BY avg_availability DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH asset_type_availability AS (SELECT `asset_type`, MEASURE(`avg_availability`) AS `avg_availability` FROM logistics_demos_catalog.mach_asset_health.asset_health_monthly_metrics WHERE `asset_type` IS NOT NULL GROUP BY ALL), ranked_types AS (SELECT `asset_type`, `avg_availability`, RANK() OVER (ORDER BY `avg_availability` DESC) AS rk FROM asset_type_availability) SELECT `asset_type`, `avg_availability` FROM ranked_types WHERE rk = 1
```

### machinery/asset_health — BAD

**Q:** Which type of industrial assets have the highest average bearing temperature in celsius?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT asset_type, AVG(bearing_temperature_c) AS total_bearing_temperature_c
FROM logistics_demos_catalog.mach_asset_health.condition_readings
GROUP BY asset_type
ORDER BY total_bearing_temperature_c DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH avg_temp_by_type AS (SELECT `asset_type`, MEASURE(`avg_bearing_temp`) AS `avg_bearing_temp` FROM logistics_demos_catalog.mach_asset_health.condition_readings_metrics WHERE `asset_type` IS NOT NULL GROUP BY ALL) SELECT `asset_type`, `avg_bearing_temp` FROM (SELECT `asset_type`, `avg_bearing_temp`, RANK() OVER (ORDER BY `avg_bearing_temp` DESC) AS `rank` FROM avg_temp_by_type) WHERE `rank` = 1
```

### machinery/asset_health — BAD

**Q:** How has highest average health index changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT summary_month, MEASURE(max_avg_health_index) AS max_avg_health_index
FROM logistics_demos_catalog.mach_asset_health.asset_health_monthly_metrics
GROUP BY ALL
ORDER BY summary_month
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `summary_month`, MEASURE(`max_avg_health_index`) AS `max_avg_health_index`
FROM logistics_demos_catalog.mach_asset_health.asset_health_monthly_metrics
GROUP BY ALL
ORDER BY `summary_month`
```

### machinery/asset_health — BAD

**Q:** How has highest vibration velocity mm s changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT reading_date, MEASURE(max_vibration_velocity_mm_s) AS max_vibration_velocity_mm_s
FROM logistics_demos_catalog.mach_asset_health.condition_readings_metrics
GROUP BY ALL
ORDER BY reading_date
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `reading_date`, MEASURE(`max_vibration_velocity_mm_s`) AS `max_vibration_velocity_mm_s`
FROM `logistics_demos_catalog`.`mach_asset_health`.`condition_readings_metrics`
GROUP BY ALL
ORDER BY `reading_date`
```

### logistics/load_demand_forecasting — BAD

**Q:** Which lane categorys have the highest number of forecast records?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT lane_category, MEASURE(forecast_record_count) AS forecast_record_count
FROM logistics_demos_catalog.load_forecasting.demand_forecasts_metrics
GROUP BY ALL
ORDER BY forecast_record_count DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH category_counts AS (SELECT `lane_category`, MEASURE(`forecast_record_count`) AS `forecast_record_count` FROM logistics_demos_catalog.load_forecasting.demand_forecasts_metrics WHERE `lane_category` IS NOT NULL GROUP BY ALL) SELECT `lane_category`, `forecast_record_count` FROM (SELECT `lane_category`, `forecast_record_count`, RANK() OVER (ORDER BY `forecast_record_count` DESC) AS `rank` FROM category_counts) WHERE `rank` = 1
```

### logistics/load_demand_forecasting — BAD

**Q:** Which lane category: domestic ftl, domestic ltl, cross-border, intermodals have the highest total freight charge?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT lane_category, SUM(freight_charge_usd) AS total_metric
FROM logistics_demos_catalog.load_forecasting.shipment_orders
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `lane_category`, MEASURE(`total_freight_revenue`) AS `total_freight_revenue`
FROM `logistics_demos_catalog`.`load_forecasting`.`shipment_orders_metrics`
WHERE `lane_category` IS NOT NULL
GROUP BY ALL
ORDER BY `total_freight_revenue` DESC
```

### logistics/load_demand_forecasting — BAD

**Q:** How has total weight delivered lbs count changed over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT order_date, MEASURE(total_weight_delivered_lbs) AS total_weight_delivered_lbs
FROM logistics_demos_catalog.load_forecasting.shipment_orders_metrics
GROUP BY ALL
ORDER BY order_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `order_date`) AS `month`, MEASURE(`total_weight_delivered_lbs`) AS `total_weight_delivered_lbs`
FROM `logistics_demos_catalog`.`load_forecasting`.`shipment_orders_metrics`
WHERE `order_date` IS NOT NULL
GROUP BY ALL
ORDER BY `month`
```

### machinery/manufacturing_resource_planning — BAD

**Q:** Which materials have the highest shortage hours this month?

**Reasons:**
- EMPTY_RESULT

**Expected SQL:**
```sql
SELECT material_name, SUM(shortage_hours) AS total_shortage_hours
FROM logistics_demos_catalog.mach_mrp.material_requirements
WHERE DATE_TRUNC('month', requirement_date) = DATE_TRUNC('month', CURRENT_DATE())
GROUP BY material_name
ORDER BY total_shortage_hours DESC
LIMIT 10
```

**Generated SQL:**
```sql
Your question refers to "materials" and "shortage hours," but there are no columns related to materials or shortage hours in the available tables. Please clarify your request using the available schema, such as work centers, capacity, or work orders.
```

### machinery/manufacturing_resource_planning — BAD

**Q:** How has highest schedule adherence percent changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT kpi_month, MEASURE(max_schedule_adherence_percent) AS max_schedule_adherence_percent
FROM logistics_demos_catalog.mach_mrp.planning_kpi_metrics
GROUP BY ALL
ORDER BY kpi_month
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `kpi_month`, MAX(`schedule_adherence_pct`) AS `max_schedule_adherence_pct`
FROM `logistics_demos_catalog`.`mach_mrp`.`planning_kpi_monthly`
GROUP BY `kpi_month`
ORDER BY `kpi_month`
```

### machinery/manufacturing_resource_planning — BAD

**Q:** How many distinct forecasts appeared each month?

**Reasons:**
- EMPTY_RESULT

**Expected SQL:**
```sql
SELECT DATE_TRUNC('month', kpi_month) AS month, COUNT(DISTINCT forecast) AS distinct_count
FROM logistics_demos_catalog.mach_mrp.planning_kpi_monthly
GROUP BY 1
ORDER BY 1
```

### machinery/spare_part_inventory_optimization — BAD

**Q:** Which spare part numbers have the highest average inventory turnover ratio?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
WITH avg_turnover AS ( SELECT part_number, AVG(turnover_ratio) AS avg_turnover_ratio
FROM logistics_demos_catalog.mach_spare_parts.inventory_kpi_monthly
WHERE part_number IS NOT NULL AND turnover_ratio IS NOT NULL
GROUP BY part_number ) SELECT part_number, avg_turnover_ratio
FROM ( SELECT *, RANK() OVER (ORDER BY avg_turnover_ratio DESC) AS rank
FROM avg_turnover ) ranked
WHERE rank = 1
LIMIT 10
```

**Generated SQL:**
```sql
SELECT part_number, AVG(turnover_ratio) AS avg_turnover_ratio, RANK() OVER (ORDER BY AVG(turnover_ratio) DESC) AS turnover_rank
FROM logistics_demos_catalog.mach_spare_parts.inventory_kpi_monthly
WHERE part_number IS NOT NULL AND turnover_ratio IS NOT NULL
GROUP BY part_number
ORDER BY avg_turnover_ratio DESC
```

### machinery/spare_part_inventory_optimization — BAD

**Q:** Which spare part numbers have the highest total quantity on hand?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT part_number, SUM(on_hand_qty) AS total_metric
FROM logistics_demos_catalog.mach_spare_parts.inventory_snapshots
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_parts AS (
  SELECT 
    `part_number`, 
    SUM(`on_hand_qty`) AS `total_on_hand_qty`,
    RANK() OVER (ORDER BY SUM(`on_hand_qty`) DESC) AS `rank`
  FROM logistics_demos_catalog.mach_spare_parts.inventory_snapshots
  WHERE `part_number` IS NOT NULL AND `on_hand_qty` IS NOT NULL
  GROUP BY `part_number`
)
SELECT `part_number`, `total_on_hand_qty`
FROM ranked_parts
WHERE `rank` = 1
```

### machinery/spare_part_inventory_optimization — BAD

**Q:** What are the top spare part number by total transaction quantity?

**Reasons:**
- EMPTY_RESULT

**Expected SQL:**
```sql
SELECT part_number, SUM(quantity) AS total_quantity
FROM logistics_demos_catalog.mach_spare_parts.parts_transactions
GROUP BY part_number ORDER BY total_quantity DESC LIMIT 10
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_parts AS (
  SELECT
    `part_number`,
    SUM(`quantity`) AS `total_quantity`,
    RANK() OVER (ORDER BY SUM(`quantity`) DESC) AS `quantity_rank`
  FROM `logistics_demos_catalog`.`mach_spare_parts`.`parts_transactions`
  WHERE `part_number` IS NOT NULL
  GROUP BY `part_number`
)
SELECT `part_number`, `total_quantity`
FROM ranked_parts
WHERE `quantity_rank` <= 10
ORDER BY `quantity_rank`
```

### machinery/spare_part_inventory_optimization — NEEDS_REVIEW

**Q:** How has average inventory turnover ratio trended by month?

**Reasons:**
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT kpi_month, MEASURE(avg_turnover) AS avg_turnover
FROM logistics_demos_catalog.mach_spare_parts.inventory_kpi_metrics
GROUP BY ALL
ORDER BY kpi_month
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `kpi_month`) AS `month`, AVG(`turnover_ratio`) AS `avg_turnover_ratio`
FROM `logistics_demos_catalog`.`mach_spare_parts`.`inventory_kpi_monthly`
WHERE `turnover_ratio` IS NOT NULL AND `kpi_month` IS NOT NULL
GROUP BY `month`
ORDER BY `month`
```

### machinery/spare_part_inventory_optimization — BAD

**Q:** How has total received qty count changed over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT txn_date, MEASURE(total_received_qty) AS total_received_qty
FROM logistics_demos_catalog.mach_spare_parts.parts_transactions_metrics
GROUP BY ALL
ORDER BY txn_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `txn_date`) AS `month`, MEASURE(`total_received_qty`) AS `total_received_qty`
FROM `logistics_demos_catalog`.`mach_spare_parts`.`parts_transactions_metrics`
WHERE `txn_date` IS NOT NULL
GROUP BY ALL
ORDER BY `month`
```

### machinery/demand_forecasting — BAD

**Q:** Which equipment models have the highest average forecast accuracy?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INSTRUCTION_COMPLIANCE_OR_MISSING_BUSINESS_LOGIC

**Expected SQL:**
```sql
WITH avg_accuracy AS ( SELECT equipment_model, AVG(forecast_accuracy_pct) AS avg_forecast_accuracy
FROM logistics_demos_catalog.mach_demand_forecast.demand_kpi_monthly
WHERE equipment_model IS NOT NULL AND forecast_accuracy_pct IS NOT NULL
GROUP BY equipment_model ) SELECT equipment_model, avg_forecast_accuracy
FROM ( SELECT *, RANK() OVER (ORDER BY avg_forecast_accuracy DESC) as rank
FROM avg_accuracy ) ranked
WHERE rank = 1
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_models AS (
  SELECT 
    `equipment_model`, 
    AVG(`forecast_accuracy_pct`) AS `avg_forecast_accuracy_pct`,
    RANK() OVER (ORDER BY AVG(`forecast_accuracy_pct`) DESC) AS `rank`
  FROM `logistics_demos_catalog`.`mach_demand_forecast`.`demand_kpi_monthly`
  WHERE `equipment_model` IS NOT NULL AND `forecast_accuracy_pct` IS NOT NULL
  GROUP BY `equipment_model`
)
SELECT `equipment_model`, `avg_forecast_accuracy_pct`
FROM ranked_models
WHERE `rank` <= 10
ORDER BY `avg_forecast_accuracy_pct` DESC
```

### machinery/demand_forecasting — BAD

**Q:** What are the top equipment model ordered by total units ordered?

**Reasons:**
- EMPTY_RESULT

**Expected SQL:**
```sql
SELECT equipment_model, SUM(order_quantity) AS total_order_quantity
FROM logistics_demos_catalog.mach_demand_forecast.customer_orders
GROUP BY equipment_model ORDER BY total_order_quantity DESC LIMIT 10
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_models AS (
  SELECT 
    `equipment_model`, 
    SUM(`order_quantity`) AS `total_units_ordered`,
    RANK() OVER (ORDER BY SUM(`order_quantity`) DESC) AS `model_rank`
  FROM `logistics_demos_catalog`.`mach_demand_forecast`.`customer_orders`
  WHERE `equipment_model` IS NOT NULL
  GROUP BY `equipment_model`
)
SELECT `equipment_model`, `total_units_ordered`
FROM ranked_models
WHERE `model_rank` <= 10
ORDER BY `model_rank`
```

### machinery/demand_forecasting — BAD

**Q:** How has highest order quantity changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT order_date, MEASURE(max_order_quantity) AS max_order_quantity
FROM logistics_demos_catalog.mach_demand_forecast.customer_orders_metrics
GROUP BY ALL
ORDER BY order_date
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `order_date`, MEASURE(`max_order_quantity`) AS `max_order_quantity`
FROM `logistics_demos_catalog`.`mach_demand_forecast`.`customer_orders_metrics`
GROUP BY ALL
ORDER BY `order_date`
```

### machinery/working_capital_cash_flow_optimization — BAD

**Q:** Which business unit types have the highest total transaction amount?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT business_unit_type, SUM(amount_usd) AS total_amount_usd
FROM logistics_demos_catalog.mach_working_capital.cash_flow_transactions
GROUP BY business_unit_type
ORDER BY total_amount_usd DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_bu_types AS (
  SELECT 
    business_unit_type, 
    SUM(amount_usd) AS total_amount_usd,
    RANK() OVER (ORDER BY SUM(amount_usd) DESC) AS rnk
  FROM logistics_demos_catalog.mach_working_capital.cash_flow_transactions
  WHERE business_unit_type IS NOT NULL
  GROUP BY business_unit_type
)
SELECT business_unit_type, total_amount_usd
FROM ranked_bu_types
WHERE rnk = 1
```

### machinery/working_capital_cash_flow_optimization — BAD

**Q:** How has highest net cash flow changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT kpi_month, MEASURE(max_net_cash_flow) AS max_net_cash_flow
FROM logistics_demos_catalog.mach_working_capital.cash_flow_kpi_metrics
GROUP BY ALL
ORDER BY kpi_month
LIMIT 10
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `kpi_month`) AS `month`, MEASURE(`max_net_cash_flow`) AS `max_net_cash_flow`
FROM `logistics_demos_catalog`.`mach_working_capital`.`cash_flow_kpi_metrics`
GROUP BY ALL
ORDER BY `month`
```

### machinery/working_capital_cash_flow_optimization — BAD

**Q:** How has total outflow count changed over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_METRIC_CALCULATION

**Expected SQL:**
```sql
SELECT txn_date, MEASURE(total_outflow) AS total_outflow
FROM logistics_demos_catalog.mach_working_capital.cash_flow_txn_metrics
GROUP BY ALL
ORDER BY txn_date
```

**Generated SQL:**
```sql
SELECT `txn_date`, MEASURE(`total_transactions`) AS `total_outflow_count`
FROM `logistics_demos_catalog`.`mach_working_capital`.`cash_flow_txn_metrics`
WHERE `direction` = 'Outflow'
GROUP BY ALL
ORDER BY `txn_date`
```

### machinery/working_capital_cash_flow_optimization — BAD

**Q:** What is the monthly trend in total inflow count?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_METRIC_CALCULATION

**Expected SQL:**
```sql
SELECT DATE_TRUNC('month', txn_date) AS month, MEASURE(total_inflow) AS total_inflow
FROM logistics_demos_catalog.mach_working_capital.cash_flow_txn_metrics
GROUP BY ALL
ORDER BY month
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `txn_date`) AS `month`, MEASURE(`total_transactions`) AS `total_inflow_count`
FROM `logistics_demos_catalog`.`mach_working_capital`.`cash_flow_txn_metrics`
WHERE `direction` = 'Inflow'
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_upstream/predictive_maintenance_asset_health — BAD

**Q:** Which fields have the highest average average health score?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT field_name, AVG(avg_health_score) AS avg_metric
FROM logistics_demos_catalog.og_upstream_pred_maint.equipment_health_monthly
GROUP BY 1
ORDER BY avg_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_fields AS (
  SELECT 
    `field_name`, 
    AVG(`avg_health_score`) AS `avg_avg_health_score`,
    RANK() OVER (ORDER BY AVG(`avg_health_score`) DESC) AS rnk
  FROM logistics_demos_catalog.og_upstream_pred_maint.equipment_health_monthly
  WHERE `field_name` IS NOT NULL AND `avg_health_score` IS NOT NULL
  GROUP BY `field_name`
)
SELECT `field_name`, `avg_avg_health_score`
FROM ranked_fields
WHERE rnk = 1
```

### oil_gas_upstream/predictive_maintenance_asset_health — BAD

**Q:** Which equipments have the highest total events?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
WITH ranked_equipment AS ( SELECT equipment_id, MEASURE(total_events) AS total_events, RANK() OVER (ORDER BY MEASURE(total_events) DESC) AS rnk
FROM logistics_demos_catalog.og_upstream_pred_maint.maintenance_events_metrics
GROUP BY ALL ) SELECT equipment_id, total_events
FROM ranked_equipment
WHERE rnk <= 10
ORDER BY total_events DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `equipment_id`, MEASURE(`total_events`) AS `total_events`
FROM `logistics_demos_catalog`.`og_upstream_pred_maint`.`maintenance_events_metrics`
WHERE `equipment_id` IS NOT NULL
GROUP BY ALL
ORDER BY `total_events` DESC
```

### oil_gas_upstream/predictive_maintenance_asset_health — BAD

**Q:** Which equipment types have the best average health score 0-100?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT equipment_type, AVG(health_score) AS avg_metric
FROM logistics_demos_catalog.og_upstream_pred_maint.sensor_readings
GROUP BY 1
ORDER BY avg_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `equipment_type`, MEASURE(`avg_health_score`) AS `avg_health_score`
FROM `logistics_demos_catalog`.`og_upstream_pred_maint`.`sensor_readings_metrics`
WHERE `equipment_type` IS NOT NULL
GROUP BY ALL
ORDER BY `avg_health_score` DESC
```

### oil_gas_upstream/predictive_maintenance_asset_health — BAD

**Q:** How has highest downtime hours changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT event_date, MEASURE(max_downtime_hours) AS max_downtime_hours
FROM logistics_demos_catalog.og_upstream_pred_maint.maintenance_events_metrics
GROUP BY ALL
ORDER BY event_date
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `event_date`, MAX(`downtime_hours`) AS `max_downtime_hours`
FROM `logistics_demos_catalog`.`og_upstream_pred_maint`.`maintenance_events`
GROUP BY `event_date`
ORDER BY `event_date`
```

### oil_gas_upstream/predictive_maintenance_asset_health — BAD

**Q:** Which equipments have the highest total sensor value?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT equipment_name, SUM(sensor_value) AS total_metric
FROM logistics_demos_catalog.og_upstream_pred_maint.sensor_readings
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `equipment_id`, MEASURE(`total_sensor_value`) AS `total_sensor_value`
FROM logistics_demos_catalog.og_upstream_pred_maint.sensor_readings_metrics
WHERE `equipment_id` IS NOT NULL
GROUP BY ALL
ORDER BY `total_sensor_value` DESC
```

### machinery/financial_analytics_reporting — BAD

**Q:** What is the average variance percentage by cost center type?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_METRIC_CALCULATION

**Expected SQL:**
```sql
SELECT cost_center_type, AVG(variance_pct) AS avg_variance_pct
FROM logistics_demos_catalog.mach_financial_analytics.budget_snapshots
GROUP BY cost_center_type ORDER BY avg_variance_pct DESC
```

**Generated SQL:**
```sql
SELECT `cost_center_type`, AVG(try_divide(100.0 * `variance_usd`,NULLIF(`budget_amount_usd`, 0))) AS `avg_variance_pct` FROM `logistics_demos_catalog`.`mach_financial_analytics`.`budget_snapshots` WHERE `cost_center_type` IS NOT NULL AND `budget_amount_usd` IS NOT NULL GROUP BY `cost_center_type` ORDER BY `avg_variance_pct` DESC
```

### machinery/financial_analytics_reporting — BAD

**Q:** Which cost center types have the highest total transaction amount?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_FILTER

**Expected SQL:**
```sql
SELECT cost_center_type, SUM(amount_usd) AS total_amount_usd
FROM logistics_demos_catalog.mach_financial_analytics.financial_transactions
GROUP BY cost_center_type
ORDER BY total_amount_usd DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `cost_center_type`, SUM(`amount_usd`) AS `total_amount_usd`
FROM `logistics_demos_catalog`.`mach_financial_analytics`.`financial_transactions`
WHERE `posting_status` = 'Posted'
GROUP BY `cost_center_type`
ORDER BY `total_amount_usd` DESC
LIMIT 10
```

### machinery/financial_analytics_reporting — BAD

**Q:** How has highest gross margin percent changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT kpi_month, MEASURE(max_gross_margin_percent) AS max_gross_margin_percent
FROM logistics_demos_catalog.mach_financial_analytics.financial_kpi_metrics
GROUP BY ALL
ORDER BY kpi_month
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `kpi_month`, MEASURE(`max_gross_margin_percent`) AS `max_gross_margin_percent`
FROM `logistics_demos_catalog`.`mach_financial_analytics`.`financial_kpi_metrics`
GROUP BY ALL
ORDER BY `kpi_month`
```

### machinery/financial_analytics_reporting — BAD

**Q:** How has total opex trended over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT txn_date, MEASURE(total_opex) AS total_opex
FROM logistics_demos_catalog.mach_financial_analytics.financial_txn_metrics
GROUP BY ALL
ORDER BY txn_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `txn_date`) AS `month`, MEASURE(`total_opex`) AS `total_opex`
FROM logistics_demos_catalog.mach_financial_analytics.financial_txn_metrics
GROUP BY ALL
ORDER BY `month`
```

### machinery/financial_analytics_reporting — BAD

**Q:** What is the monthly trend in total monthly revenue?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT DATE_TRUNC('month', txn_date) AS month, MEASURE(total_revenue) AS total_revenue
FROM logistics_demos_catalog.mach_financial_analytics.financial_txn_metrics
GROUP BY ALL
ORDER BY month
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `kpi_month`) AS `month`, MEASURE(`total_revenue`) AS `total_monthly_revenue`
FROM logistics_demos_catalog.mach_financial_analytics.financial_kpi_metrics
GROUP BY ALL
ORDER BY `month`
```

### machinery/spend_intelligence — BAD

**Q:** How has highest total spend changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT kpi_month, MEASURE(max_total_spend) AS max_total_spend
FROM logistics_demos_catalog.mach_spend_intel.spend_kpi_metrics
GROUP BY ALL
ORDER BY kpi_month
LIMIT 10
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `kpi_month`) AS `month`, MEASURE(`max_total_spend`) AS `highest_total_spend`
FROM `logistics_demos_catalog`.`mach_spend_intel`.`spend_kpi_metrics`
GROUP BY ALL
ORDER BY `month`
```

### machinery/spend_intelligence — BAD

**Q:** How has late delivery count changed over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT po_date, MEASURE(late_delivery_count) AS late_delivery_count
FROM logistics_demos_catalog.mach_spend_intel.procurement_metrics
GROUP BY ALL
ORDER BY po_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `po_date`) AS `month`, MEASURE(`late_delivery_count`) AS `late_delivery_count`
FROM `logistics_demos_catalog`.`mach_spend_intel`.`procurement_metrics`
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_upstream/reservoir_management — BAD

**Q:** How does opex per boe compare across formations?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_METRIC_CALCULATION

**Expected SQL:**
```sql
SELECT formation, SUM(opex_per_boe) AS total_metric
FROM logistics_demos_catalog.og_upstream_reservoir.reservoir_kpi_monthly
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `formation`, AVG(`opex_per_boe`) AS `avg_opex_per_boe`
FROM `logistics_demos_catalog`.`og_upstream_reservoir`.`reservoir_kpi_monthly`
WHERE `formation` IS NOT NULL AND `opex_per_boe` IS NOT NULL
GROUP BY `formation`
ORDER BY `avg_opex_per_boe` ASC
```

### oil_gas_upstream/reservoir_management — BAD

**Q:** What has been the peak cumulative oil barrels each month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT snapshot_date, MEASURE(max_cumulative_oil_bbl) AS max_cumulative_oil_bbl
FROM logistics_demos_catalog.og_upstream_reservoir.reservoir_snapshots_metrics
GROUP BY ALL
ORDER BY snapshot_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `snapshot_date`) AS `month`, MEASURE(`max_cumulative_oil_bbl`) AS `peak_cumulative_oil_bbl`
FROM logistics_demos_catalog.og_upstream_reservoir.reservoir_snapshots_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_upstream/reservoir_management — BAD

**Q:** How has average oil barrels per day trended by month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT reading_date, MEASURE(avg_oil_bpd) AS avg_oil_bpd
FROM logistics_demos_catalog.og_upstream_reservoir.production_readings_metrics
GROUP BY ALL
ORDER BY reading_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `reading_date`) AS `month`, MEASURE(`avg_oil_bpd`) AS `avg_oil_bpd`
FROM `logistics_demos_catalog`.`og_upstream_reservoir`.`production_readings_metrics`
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_upstream/well_production_monitoring_flow — BAD

**Q:** Which wells have the highest average water cut percentage?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT well_name, AVG(water_cut_pct) AS avg_water_cut_pct
FROM logistics_demos_catalog.og_upstream_wellflow.well_status_snapshots
GROUP BY well_name
ORDER BY avg_water_cut_pct DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `well_id`, MEASURE(`avg_water_cut`) AS `avg_water_cut`
FROM logistics_demos_catalog.og_upstream_wellflow.well_status_snapshots_metrics
GROUP BY ALL
ORDER BY `avg_water_cut` DESC
LIMIT 10
```

### oil_gas_upstream/well_production_monitoring_flow — BAD

**Q:** Which wells have the highest average oil barrels per day?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT well_name, AVG(oil_bpd) AS total_oil_bpd
FROM logistics_demos_catalog.og_upstream_wellflow.production_readings
GROUP BY well_name
ORDER BY total_oil_bpd DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_wells AS (
  SELECT `well_id`, MEASURE(`avg_oil_bpd`) AS `avg_oil_bpd`,
         RANK() OVER (ORDER BY MEASURE(`avg_oil_bpd`) DESC) AS `rank`
  FROM logistics_demos_catalog.og_upstream_wellflow.production_readings_metrics
  GROUP BY ALL
)
SELECT `well_id`, `avg_oil_bpd`
FROM ranked_wells
WHERE `rank` <= 10
ORDER BY `avg_oil_bpd` DESC
```

### oil_gas_upstream/well_production_monitoring_flow — BAD

**Q:** What has been the peak cumulative oil barrels each month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT snapshot_date, MEASURE(max_cumulative_oil_bbl) AS max_cumulative_oil_bbl
FROM logistics_demos_catalog.og_upstream_wellflow.well_status_snapshots_metrics
GROUP BY ALL
ORDER BY snapshot_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `snapshot_date`) AS `month`, MEASURE(`max_cumulative_oil_bbl`) AS `peak_cumulative_oil_bbl`
FROM logistics_demos_catalog.og_upstream_wellflow.well_status_snapshots_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_upstream/well_production_monitoring_flow — BAD

**Q:** How has average oil barrels per day trended by month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT reading_date, MEASURE(avg_oil_bpd) AS avg_oil_bpd
FROM logistics_demos_catalog.og_upstream_wellflow.production_readings_metrics
GROUP BY ALL
ORDER BY reading_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `reading_date`) AS `month`, MEASURE(`avg_oil_bpd`) AS `avg_oil_bpd`
FROM logistics_demos_catalog.og_upstream_wellflow.production_readings_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_midstream/regulation_compliance — BAD

**Q:** Which compliance areas have the highest total violations in month?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_MISINTERPRETATION_OF_USER_REQUEST

**Expected SQL:**
```sql
SELECT compliance_area, SUM(violation_count) AS total_metric
FROM logistics_demos_catalog.og_midstream_compliance.compliance_kpi_monthly
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH monthly_violations AS (
  SELECT DATE_TRUNC('month', `event_date`) AS `month`,
         `compliance_area`,
         MEASURE(`violation_count`) AS `total_violations`
  FROM logistics_demos_catalog.og_midstream_compliance.compliance_events_metrics
  GROUP BY ALL
)
SELECT `month`, `compliance_area`, `total_violations`
FROM (
  SELECT *,
         RANK() OVER (PARTITION BY `month` ORDER BY `total_violations` DESC) AS rnk
  FROM monthly_violations
)
WHERE rnk = 1
ORDER BY `month`
```

### oil_gas_midstream/regulation_compliance — BAD

**Q:** Which regulations have the highest total fine/penalty amount?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT regulation_name, SUM(fine_amount_usd) AS total_fine_amount_usd
FROM logistics_demos_catalog.og_midstream_compliance.compliance_events
GROUP BY regulation_name
ORDER BY total_fine_amount_usd DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `reg_id`, SUM(`fine_amount_usd`) AS `total_fine_amount_usd`
FROM `logistics_demos_catalog`.`og_midstream_compliance`.`compliance_events`
WHERE `reg_id` IS NOT NULL
GROUP BY `reg_id`
ORDER BY `total_fine_amount_usd` DESC
```

### oil_gas_midstream/regulation_compliance — BAD

**Q:** How has average compliance score 0-100 trended by month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT snapshot_date, MEASURE(avg_compliance_score) AS avg_compliance_score
FROM logistics_demos_catalog.og_midstream_compliance.audit_snapshots_metrics
GROUP BY ALL
ORDER BY snapshot_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `snapshot_date`) AS `month`, MEASURE(`avg_compliance_score`) AS `avg_compliance_score`
FROM logistics_demos_catalog.og_midstream_compliance.audit_snapshots_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_midstream/logistics_optimization — BAD

**Q:** Which pipeline types have the highest total monthly revenue?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT pipeline_type, SUM(revenue_usd) AS total_metric
FROM logistics_demos_catalog.og_midstream_logistics.throughput_kpi_monthly
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `kpi_month`) AS `month`, `pipeline_type`, SUM(`revenue_usd`) AS `total_revenue`
FROM `logistics_demos_catalog`.`og_midstream_logistics`.`throughput_kpi_monthly`
WHERE `pipeline_type` IS NOT NULL AND `revenue_usd` IS NOT NULL
GROUP BY DATE_TRUNC('month', `kpi_month`), `pipeline_type`
ORDER BY `total_revenue` DESC
```

### oil_gas_midstream/logistics_optimization — BAD

**Q:** Which segments have the best average throughput in barrels per day?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT segment_name, AVG(throughput_bpd) AS total_throughput_bpd
FROM logistics_demos_catalog.og_midstream_logistics.flow_readings
GROUP BY segment_name
ORDER BY total_throughput_bpd DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_segments AS (
  SELECT 
    `segment_id`,
    MEASURE(`avg_throughput_bpd`) AS `average_throughput_bpd`,
    RANK() OVER (ORDER BY MEASURE(`avg_throughput_bpd`) DESC) AS throughput_rank
  FROM `logistics_demos_catalog`.`og_midstream_logistics`.`flow_readings_metrics`
  WHERE `segment_id` IS NOT NULL
  GROUP BY ALL
)
SELECT `segment_id`, `average_throughput_bpd`
FROM ranked_segments
WHERE throughput_rank <= 10
ORDER BY throughput_rank;
```

### oil_gas_midstream/logistics_optimization — BAD

**Q:** How has total volume impact barrels trended over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_METRIC_CALCULATION

**Expected SQL:**
```sql
SELECT event_date, MEASURE(max_volume_impact_bbl) AS max_volume_impact_bbl
FROM logistics_demos_catalog.og_midstream_logistics.logistics_events_metrics
GROUP BY ALL
ORDER BY event_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `event_date`) AS `month`, MEASURE(`total_volume_impact_bbl`) AS `total_volume_impact_bbl`
FROM logistics_demos_catalog.og_midstream_logistics.logistics_events_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_midstream/logistics_optimization — BAD

**Q:** Which segments have the best average capacity utilization percentage?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT segment_name, AVG(capacity_utilization_pct) AS avg_metric
FROM logistics_demos_catalog.og_midstream_logistics.flow_readings
GROUP BY 1
ORDER BY avg_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `segment_id`, MEASURE(`avg_utilization_pct`) AS `avg_utilization_pct`
FROM `logistics_demos_catalog`.`og_midstream_logistics`.`flow_readings_metrics`
WHERE `segment_id` IS NOT NULL
GROUP BY ALL
HAVING avg_utilization_pct IS NOT NULL
ORDER BY `avg_utilization_pct` DESC
```

### oil_gas_midstream/logistics_optimization — BAD

**Q:** How has average throughput in barrels per day trended by month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT reading_date, MEASURE(avg_throughput_bpd) AS avg_throughput_bpd
FROM logistics_demos_catalog.og_midstream_logistics.flow_readings_metrics
GROUP BY ALL
ORDER BY reading_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `reading_date`) AS `month`, MEASURE(`avg_throughput_bpd`) AS `avg_throughput_bpd`
FROM logistics_demos_catalog.og_midstream_logistics.flow_readings_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_midstream/energy_trading — BAD

**Q:** Which commoditys have the highest total sharpe ratio?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT commodity, SUM(sharpe_ratio) AS total_metric
FROM logistics_demos_catalog.og_midstream_trading.trading_kpi_monthly
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_commodities AS (
  SELECT 
    `commodity`, 
    SUM(`sharpe_ratio`) AS total_sharpe_ratio,
    RANK() OVER (ORDER BY SUM(`sharpe_ratio`) DESC) AS sharpe_rank
  FROM `logistics_demos_catalog`.`og_midstream_trading`.`trading_kpi_monthly`
  WHERE `commodity` IS NOT NULL AND `sharpe_ratio` IS NOT NULL
  GROUP BY `commodity`
)
SELECT `commodity`, total_sharpe_ratio
FROM ranked_commodities
WHERE sharpe_rank = 1
```

### oil_gas_midstream/energy_trading — BAD

**Q:** Which contracts have the highest total volume in barrels equivalent?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT contract_name, SUM(volume_bbl) AS total_volume_bbl
FROM logistics_demos_catalog.og_midstream_trading.trade_transactions
GROUP BY contract_name
ORDER BY total_volume_bbl DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_contracts AS (
  SELECT 
    `contract_id`, 
    MEASURE(`total_volume_bbl`) AS `total_volume_bbl`,
    RANK() OVER (ORDER BY MEASURE(`total_volume_bbl`) DESC) AS rk
  FROM logistics_demos_catalog.og_midstream_trading.trade_transactions_metrics
  WHERE `contract_id` IS NOT NULL
  GROUP BY ALL
)
SELECT `contract_id`, `total_volume_bbl`
FROM ranked_contracts
WHERE rk <= 10
ORDER BY `total_volume_bbl` DESC
```

### oil_gas_midstream/energy_trading — BAD

**Q:** How has average net position barrels trended by month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT snapshot_date, MEASURE(avg_net_position_bbl) AS avg_net_position_bbl
FROM logistics_demos_catalog.og_midstream_trading.position_snapshots_metrics
GROUP BY ALL
ORDER BY snapshot_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `snapshot_date`) AS `month`, MEASURE(`avg_net_position_bbl`) AS `average_net_position_bbl`
FROM `logistics_demos_catalog`.`og_midstream_trading`.`position_snapshots_metrics`
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_midstream/energy_trading — BAD

**Q:** How has total volume in barrels equivalent trended over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_METRIC_CALCULATION

**Expected SQL:**
```sql
SELECT trade_date, MEASURE(max_volume_bbl) AS max_volume_bbl
FROM logistics_demos_catalog.og_midstream_trading.trade_transactions_metrics
GROUP BY ALL
ORDER BY trade_date
```

**Generated SQL:**
```sql
SELECT `trade_date`, MEASURE(`total_volume_bbl`) AS `total_volume_bbl`
FROM `logistics_demos_catalog`.`og_midstream_trading`.`trade_transactions_metrics`
GROUP BY ALL
ORDER BY `trade_date`
```

### oil_gas_midstream/scenario_planning_business_simulation — BAD

**Q:** Which scenarios have the highest total revenue impact millions?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT scenario_name, SUM(revenue_impact_mm) AS total_revenue_impact_mm
FROM logistics_demos_catalog.og_midstream_scenario.simulation_runs
GROUP BY scenario_name
ORDER BY total_revenue_impact_mm DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_scenarios AS (
  SELECT 
    `scenario_id`,
    MEASURE(`avg_revenue_impact_mm`) AS `total_revenue_impact_mm`,
    RANK() OVER (ORDER BY MEASURE(`avg_revenue_impact_mm`) DESC) AS rnk
  FROM logistics_demos_catalog.og_midstream_scenario.simulation_runs_metrics
  GROUP BY ALL
)
SELECT `scenario_id`, `total_revenue_impact_mm`
FROM ranked_scenarios
WHERE rnk <= 10
ORDER BY `total_revenue_impact_mm` DESC
```

### oil_gas_midstream/scenario_planning_business_simulation — BAD

**Q:** Which scenarios have the best average throughput change percentage?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT scenario_name, AVG(throughput_change_pct) AS avg_metric
FROM logistics_demos_catalog.og_midstream_scenario.simulation_runs
GROUP BY 1
ORDER BY avg_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH scenario_throughput AS (SELECT `scenario_id`, MEASURE(`avg_throughput_change_pct`) AS `avg_throughput_change_pct` FROM `logistics_demos_catalog`.`og_midstream_scenario`.`simulation_runs_metrics` WHERE `scenario_id` IS NOT NULL GROUP BY ALL) SELECT `scenario_id`, `avg_throughput_change_pct`, RANK() OVER (ORDER BY `avg_throughput_change_pct` DESC) AS `rank` FROM scenario_throughput WHERE `avg_throughput_change_pct` IS NOT NULL QUALIFY `rank` <= 10 ORDER BY `rank`
```

### oil_gas_midstream/scenario_planning_business_simulation — BAD

**Q:** How has highest throughput change percent changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT run_date, MEASURE(max_throughput_change_percent) AS max_throughput_change_percent
FROM logistics_demos_catalog.og_midstream_scenario.simulation_runs_metrics
GROUP BY ALL
ORDER BY run_date
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `run_date`, MEASURE(`max_throughput_change_percent`) AS `max_throughput_change_percent`
FROM `logistics_demos_catalog`.`og_midstream_scenario`.`simulation_runs_metrics`
WHERE `run_date` IS NOT NULL
GROUP BY ALL
ORDER BY `run_date`
```

### oil_gas_midstream/automated_reporting_of_carbon_intensity — BAD

**Q:** Which categorys have the highest total emissions tons?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT emission_category, SUM(total_emissions_tons) AS total_metric
FROM logistics_demos_catalog.og_midstream_carbon.carbon_kpi_monthly
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_categories AS (
  SELECT 
    `emission_category`, 
    SUM(`total_emissions_tons`) AS `total_emissions_tons`,
    RANK() OVER (ORDER BY SUM(`total_emissions_tons`) DESC) AS `rank`
  FROM `logistics_demos_catalog`.`og_midstream_carbon`.`carbon_kpi_monthly`
  WHERE `emission_category` IS NOT NULL AND `total_emissions_tons` IS NOT NULL
  GROUP BY `emission_category`
)
SELECT `emission_category`, `total_emissions_tons`
FROM ranked_categories
WHERE `rank` = 1;
```

### oil_gas_midstream/automated_reporting_of_carbon_intensity — BAD

**Q:** What are the top emission source id by total co2 equivalent metric tons?

**Reasons:**
- EMPTY_RESULT

**Expected SQL:**
```sql
SELECT source_id, SUM(co2e_metric_tons) AS total_co2e_metric_tons
FROM logistics_demos_catalog.og_midstream_carbon.emission_readings
GROUP BY source_id ORDER BY total_co2e_metric_tons DESC LIMIT 10
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_sources AS (
  SELECT 
    `source_id`, 
    MEASURE(`total_co2e_tons`) AS `total_co2e_tons`,
    RANK() OVER (ORDER BY MEASURE(`total_co2e_tons`) DESC) AS `co2e_rank`
  FROM `logistics_demos_catalog`.`og_midstream_carbon`.`emission_readings_metrics`
  WHERE `source_id` IS NOT NULL
  GROUP BY ALL
)
SELECT `source_id`, `total_co2e_tons`
FROM ranked_sources
WHERE `co2e_rank` <= 10
ORDER BY `co2e_rank`
```

### oil_gas_midstream/automated_reporting_of_carbon_intensity — BAD

**Q:** How has total monthly co2e changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT DATE_TRUNC('month', snapshot_date) AS month, MEASURE(total_monthly_co2e) AS total_monthly_co2e
FROM logistics_demos_catalog.og_midstream_carbon.carbon_snapshots_metrics
GROUP BY ALL
ORDER BY month
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `snapshot_date`) AS `month`, SUM(`monthly_co2e_tons`) AS `total_monthly_co2e_tons`
FROM `logistics_demos_catalog`.`og_midstream_carbon`.`carbon_snapshots`
GROUP BY `snapshot_date`
ORDER BY `snapshot_date`
```

### oil_gas_midstream/automated_reporting_of_carbon_intensity — BAD

**Q:** What has been the peak co2 equivalent metric tons each month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT reading_date, MEASURE(max_co2e_metric_tons) AS max_co2e_metric_tons
FROM logistics_demos_catalog.og_midstream_carbon.emission_readings_metrics
GROUP BY ALL
ORDER BY reading_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `reading_date`) AS `month`, MEASURE(`max_co2e_metric_tons`) AS `peak_co2e_metric_tons`
FROM `logistics_demos_catalog`.`og_midstream_carbon`.`emission_readings_metrics`
GROUP BY ALL
HAVING peak_co2e_metric_tons IS NOT NULL
ORDER BY `month`
```

### oil_gas_midstream/financial_analytics_reporting — BAD

**Q:** Which cost centers have the highest total actual revenue?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT cost_center_id, MEASURE(total_actual_revenue) AS total_actual_revenue
FROM logistics_demos_catalog.og_midstream_finance.budget_snapshots_metrics
GROUP BY ALL
ORDER BY total_actual_revenue DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `cost_center_id`, MEASURE(`total_actual_revenue`) AS `total_actual_revenue`
FROM `logistics_demos_catalog`.`og_midstream_finance`.`budget_snapshots_metrics`
WHERE `cost_center_id` IS NOT NULL
GROUP BY ALL
ORDER BY `total_actual_revenue` DESC
```

### oil_gas_midstream/financial_analytics_reporting — BAD

**Q:** Which cost centers have the highest total transaction amount?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT cost_center_name, SUM(amount_usd) AS total_amount_usd
FROM logistics_demos_catalog.og_midstream_finance.financial_transactions
GROUP BY cost_center_name
ORDER BY total_amount_usd DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_cost_centers AS (
  SELECT 
    `cost_center_id`, 
    MEASURE(`total_amount_usd`) AS `total_transaction_amount`,
    RANK() OVER (ORDER BY MEASURE(`total_amount_usd`) DESC) AS `rank`
  FROM logistics_demos_catalog.og_midstream_finance.financial_transactions_metrics
  WHERE `cost_center_id` IS NOT NULL
  GROUP BY ALL
)
SELECT `cost_center_id`, `total_transaction_amount`, `rank`
FROM ranked_cost_centers
WHERE `rank` = 1
ORDER BY `cost_center_id`
```

### oil_gas_midstream/financial_analytics_reporting — BAD

**Q:** How has highest amount changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT transaction_date, MEASURE(max_amount) AS max_amount
FROM logistics_demos_catalog.og_midstream_finance.financial_transactions_metrics
GROUP BY ALL
ORDER BY transaction_date
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `transaction_date`, MEASURE(`max_amount`) AS `max_amount`
FROM `logistics_demos_catalog`.`og_midstream_finance`.`financial_transactions_metrics`
WHERE `transaction_date` IS NOT NULL
GROUP BY ALL
ORDER BY `transaction_date`
```

### oil_gas_midstream/working_capital_cash_flow_optimization — BAD

**Q:** Which business units have the highest average dso?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT business_unit_id, MEASURE(avg_dso_days) AS average_dso_days
FROM logistics_demos_catalog.og_midstream_cashflow.working_capital_snapshots_metrics
WHERE business_unit_id IS NOT NULL
GROUP BY ALL
HAVING average_dso_days IS NOT NULL
ORDER BY average_dso_days DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `business_unit_id`, MEASURE(`avg_dso_days`) AS `average_dso_days`
FROM `logistics_demos_catalog`.`og_midstream_cashflow`.`working_capital_snapshots_metrics`
WHERE `business_unit_id` IS NOT NULL
GROUP BY ALL
HAVING average_dso_days IS NOT NULL
ORDER BY `average_dso_days` DESC
```

### oil_gas_midstream/working_capital_cash_flow_optimization — BAD

**Q:** Which business units have the most cash flow amount?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT business_unit_name, SUM(amount_usd) AS total_amount_usd
FROM logistics_demos_catalog.og_midstream_cashflow.cash_transactions
GROUP BY business_unit_name
ORDER BY total_amount_usd DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT business_unit_name, SUM(amount_usd) AS total_amount_usd
FROM logistics_demos_catalog.og_midstream_cashflow.cash_transactions
WHERE business_unit_name IS NOT NULL
GROUP BY business_unit_name
ORDER BY total_amount_usd DESC
```

### oil_gas_midstream/working_capital_cash_flow_optimization — BAD

**Q:** What has been the peak accounts_receivable each month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT snapshot_date, MEASURE(max_accounts_receivable) AS max_accounts_receivable
FROM logistics_demos_catalog.og_midstream_cashflow.working_capital_snapshots_metrics
GROUP BY ALL
ORDER BY snapshot_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `snapshot_date`) AS `month`, MEASURE(`max_accounts_receivable`) AS `peak_accounts_receivable_usd`
FROM `logistics_demos_catalog`.`og_midstream_cashflow`.`working_capital_snapshots_metrics`
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_midstream/working_capital_cash_flow_optimization — BAD

**Q:** How has total outflows trended over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT transaction_date, MEASURE(total_outflows_usd) AS total_outflows_usd
FROM logistics_demos_catalog.og_midstream_cashflow.cash_transactions_metrics
GROUP BY ALL
ORDER BY transaction_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `transaction_date`) AS `month`, MEASURE(`total_outflows_usd`) AS `total_outflows_usd`
FROM logistics_demos_catalog.og_midstream_cashflow.cash_transactions_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_midstream/working_capital_cash_flow_optimization — BAD

**Q:** How has total inflows trended over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT transaction_date, MEASURE(total_inflows_usd) AS total_inflows_usd
FROM logistics_demos_catalog.og_midstream_cashflow.cash_transactions_metrics
GROUP BY ALL
ORDER BY transaction_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `transaction_date`) AS `month`, MEASURE(`total_inflows_usd`) AS `total_inflows_usd`
FROM logistics_demos_catalog.og_midstream_cashflow.cash_transactions_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_refining/predictive_maintenance_asset_health — BAD

**Q:** Which equipments have the highest total raw sensor measurement value?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT equipment_name, SUM(sensor_value) AS total_sensor_value
FROM logistics_demos_catalog.og_refining_pred_maint.sensor_readings
GROUP BY equipment_name
ORDER BY total_sensor_value DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `equipment_id`, MEASURE(`total_sensor_value`) AS `total_sensor_value`
FROM logistics_demos_catalog.og_refining_pred_maint.sensor_readings_metrics
GROUP BY ALL
ORDER BY `total_sensor_value` DESC
LIMIT 10
```

### oil_gas_refining/predictive_maintenance_asset_health — BAD

**Q:** How has highest downtime hours changed over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT event_date, MEASURE(max_downtime_hours) AS max_downtime_hours
FROM logistics_demos_catalog.og_refining_pred_maint.maintenance_events_metrics
GROUP BY ALL
ORDER BY event_date
LIMIT 10
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `event_date`) AS `month`, MEASURE(`max_downtime_hours`) AS `max_downtime_hours`
FROM logistics_demos_catalog.og_refining_pred_maint.maintenance_events_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_refining/predictive_maintenance_asset_health — BAD

**Q:** Which process units have the highest average asset health score?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INSTRUCTION_COMPLIANCE_OR_MISSING_BUSINESS_LOGIC

**Expected SQL:**
```sql
WITH ranked_units AS ( SELECT process_unit, AVG(avg_health_score) AS avg_health_score, RANK() OVER (ORDER BY AVG(avg_health_score) DESC) AS rnk
FROM logistics_demos_catalog.og_refining_pred_maint.equipment_health_monthly
WHERE process_unit IS NOT NULL AND avg_health_score IS NOT NULL
GROUP BY process_unit ) SELECT process_unit, avg_health_score
FROM ranked_units
WHERE rnk = 1
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `process_unit`, AVG(`avg_health_score`) AS `average_health_score`
FROM `logistics_demos_catalog`.`og_refining_pred_maint`.`equipment_health_monthly`
WHERE `process_unit` IS NOT NULL AND `avg_health_score` IS NOT NULL
GROUP BY `process_unit`
ORDER BY `average_health_score` DESC
```

### oil_gas_midstream/spend_intelligence — BAD

**Q:** Which suppliers have the highest total transaction amount?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT supplier_name, SUM(amount_usd) AS total_metric
FROM logistics_demos_catalog.og_midstream_spend.procurement_transactions
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `supplier_id`, MEASURE(`total_spend_usd`) AS `total_transaction_amount`
FROM logistics_demos_catalog.og_midstream_spend.procurement_transactions_metrics
GROUP BY ALL
ORDER BY `total_transaction_amount` DESC
LIMIT 10
```

### oil_gas_midstream/spend_intelligence — BAD

**Q:** How has average quality score 0-100 trended by month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT snapshot_date, MEASURE(avg_quality_score) AS avg_quality_score
FROM logistics_demos_catalog.og_midstream_spend.supplier_snapshots_metrics
GROUP BY ALL
ORDER BY snapshot_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `snapshot_date`) AS `month`, MEASURE(`avg_quality_score`) AS `avg_quality_score`
FROM `logistics_demos_catalog`.`og_midstream_spend`.`supplier_snapshots_metrics`
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_midstream/spend_intelligence — BAD

**Q:** How has highest amount changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT transaction_date, MEASURE(max_amount) AS max_amount
FROM logistics_demos_catalog.og_midstream_spend.procurement_transactions_metrics
GROUP BY ALL
ORDER BY transaction_date
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `transaction_date`, MEASURE(`max_amount`) AS `max_amount_usd`
FROM `logistics_demos_catalog`.`og_midstream_spend`.`procurement_transactions_metrics`
GROUP BY ALL
ORDER BY `transaction_date`
```

### oil_gas_refining/production_monitoring — BAD

**Q:** Which process units have the best average utilization percentage?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT unit_name, AVG(avg_utilization_pct) AS avg_avg_utilization_pct
FROM logistics_demos_catalog.og_refining_production.production_snapshots
GROUP BY unit_name
ORDER BY avg_avg_utilization_pct DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_units AS (
  SELECT
    `unit_id`,
    MEASURE(`avg_utilization_pct`) AS `avg_utilization_pct`,
    RANK() OVER (ORDER BY MEASURE(`avg_utilization_pct`) DESC) AS `utilization_rank`
  FROM `logistics_demos_catalog`.`og_refining_production`.`throughput_readings_metrics`
  WHERE `unit_id` IS NOT NULL
  GROUP BY ALL
)
SELECT `unit_id`, `avg_utilization_pct`
FROM ranked_units
ORDER BY `utilization_rank`
```

### oil_gas_refining/production_monitoring — BAD

**Q:** What are the top process unit id by total throughput in barrels per day?

**Reasons:**
- EMPTY_RESULT

**Expected SQL:**
```sql
SELECT unit_id, SUM(throughput_bpd) AS total_throughput_bpd
FROM logistics_demos_catalog.og_refining_production.throughput_readings
GROUP BY unit_id ORDER BY total_throughput_bpd DESC LIMIT 10
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_units AS (
  SELECT 
    `unit_id`, 
    SUM(`throughput_bpd`) AS `total_throughput_bpd`,
    RANK() OVER (ORDER BY SUM(`throughput_bpd`) DESC) AS `throughput_rank`
  FROM `logistics_demos_catalog`.`og_refining_production`.`throughput_readings`
  WHERE `unit_id` IS NOT NULL
  GROUP BY `unit_id`
)
SELECT `unit_id`, `total_throughput_bpd`
FROM ranked_units
WHERE `throughput_rank` <= 10
ORDER BY `throughput_rank`
```

### oil_gas_refining/production_monitoring — BAD

**Q:** How has average throughput barrels per day trended by month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT snapshot_date, MEASURE(avg_throughput) AS avg_throughput
FROM logistics_demos_catalog.og_refining_production.production_snapshots_metrics
GROUP BY ALL
ORDER BY snapshot_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `reading_date`) AS `month`, MEASURE(`avg_throughput_bpd`) AS `avg_throughput_bpd`
FROM logistics_demos_catalog.og_refining_production.throughput_readings_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_refining/production_monitoring — BAD

**Q:** How has average throughput in barrels per day trended by month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT reading_date, MEASURE(avg_throughput_bpd) AS avg_throughput_bpd
FROM logistics_demos_catalog.og_refining_production.throughput_readings_metrics
GROUP BY ALL
ORDER BY reading_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `reading_date`) AS `month`, MEASURE(`avg_throughput_bpd`) AS `avg_throughput_bpd`
FROM logistics_demos_catalog.og_refining_production.throughput_readings_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_refining/energy_use_monitoring_heat — BAD

**Q:** How does peak clean ua value btu/hr/f compare across equipment categorys?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISINTERPRETATION_OF_USER_REQUEST

**Expected SQL:**
```sql
SELECT snapshot_date, MEASURE(max_clean_ua_btu_hr_f) AS max_clean_ua_btu_hr_f
FROM logistics_demos_catalog.og_refining_energy.thermal_snapshots_metrics
GROUP BY ALL
ORDER BY snapshot_date
```

**Generated SQL:**
```sql
SELECT `equipment_category`, MEASURE(`max_clean_ua_btu_hr_f`) AS `peak_clean_ua_btu_hr_f`
FROM `logistics_demos_catalog`.`og_refining_energy`.`thermal_snapshots_metrics`
WHERE `equipment_category` IS NOT NULL
GROUP BY ALL
ORDER BY `peak_clean_ua_btu_hr_f` DESC
```

### oil_gas_refining/energy_use_monitoring_heat — BAD

**Q:** What has been the peak energy consumption in mmbtu each month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT reading_date, MEASURE(max_energy_consumption_mmbtu) AS max_energy_consumption_mmbtu
FROM logistics_demos_catalog.og_refining_energy.energy_readings_metrics
GROUP BY ALL
ORDER BY reading_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `reading_date`) AS `month`, MAX(`energy_consumption_mmbtu`) AS `peak_energy_consumption_mmbtu`
FROM `logistics_demos_catalog`.`og_refining_energy`.`energy_readings`
WHERE `reading_date` IS NOT NULL AND `energy_consumption_mmbtu` IS NOT NULL
GROUP BY DATE_TRUNC('month', `reading_date`)
ORDER BY `month`
```

### oil_gas_refining/quality_event_root_cause_analysis — BAD

**Q:** Which process units have the highest total off-spec product volume in barrels?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT unit_name, SUM(off_spec_volume_bbl) AS total_off_spec_volume_bbl
FROM logistics_demos_catalog.og_refining_quality.quality_events
GROUP BY unit_name
ORDER BY total_off_spec_volume_bbl DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT unit_id, MEASURE(total_off_spec_bbl) AS total_off_spec_bbl
FROM logistics_demos_catalog.og_refining_quality.quality_events_metrics
GROUP BY ALL
ORDER BY total_off_spec_bbl DESC
LIMIT 10
```

### oil_gas_refining/quality_event_root_cause_analysis — BAD

**Q:** How has total batch volume in barrels trended over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT batch_date, MEASURE(max_volume_bbl) AS max_volume_bbl
FROM logistics_demos_catalog.og_refining_quality.batch_records_metrics
GROUP BY ALL
ORDER BY batch_date
```

**Generated SQL:**
```sql
SELECT `batch_date`, SUM(`volume_bbl`) AS `total_volume_bbl`
FROM `logistics_demos_catalog`.`og_refining_quality`.`batch_records`
GROUP BY `batch_date`
ORDER BY `batch_date`
```

### oil_gas_refining/quality_event_root_cause_analysis — BAD

**Q:** Which process units have the highest total financial impact?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT unit_name, SUM(financial_impact_usd) AS total_metric
FROM logistics_demos_catalog.og_refining_quality.quality_events
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `unit_id`, MEASURE(`total_financial_impact_usd`) AS `total_financial_impact_usd`
FROM `logistics_demos_catalog`.`og_refining_quality`.`quality_events_metrics`
WHERE `unit_id` IS NOT NULL
GROUP BY ALL
ORDER BY `total_financial_impact_usd` DESC
LIMIT 10
```

### oil_gas_refining/quality_event_root_cause_analysis — BAD

**Q:** How has total off-spec product volume in barrels trended over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_METRIC_CALCULATION

**Expected SQL:**
```sql
SELECT event_date, MEASURE(max_off_spec_volume_bbl) AS max_off_spec_volume_bbl
FROM logistics_demos_catalog.og_refining_quality.quality_events_metrics
GROUP BY ALL
ORDER BY event_date
```

**Generated SQL:**
```sql
SELECT `event_date`, SUM(`off_spec_volume_bbl`) AS `total_off_spec_volume_bbl`
FROM `logistics_demos_catalog`.`og_refining_quality`.`quality_events`
GROUP BY `event_date`
ORDER BY `event_date`
```

### oil_gas_refining/financial_analytics_reporting — BAD

**Q:** Which cost centers have the highest total transaction amount?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT cost_center_name, SUM(amount_usd) AS total_amount_usd
FROM logistics_demos_catalog.og_refining_finance.financial_transactions
GROUP BY cost_center_name
ORDER BY total_amount_usd DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_cost_centers AS (
  SELECT 
    `cost_center_id`, 
    MEASURE(`total_amount_usd`) AS `total_transaction_amount`,
    RANK() OVER (ORDER BY MEASURE(`total_amount_usd`) DESC) AS `rank`
  FROM logistics_demos_catalog.og_refining_finance.financial_transactions_metrics
  WHERE `cost_center_id` IS NOT NULL
  GROUP BY ALL
)
SELECT `cost_center_id`, `total_transaction_amount`
FROM ranked_cost_centers
WHERE `rank` <= 10
ORDER BY `total_transaction_amount` DESC
```

### oil_gas_refining/financial_analytics_reporting — BAD

**Q:** How has highest amount changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT transaction_date, MEASURE(max_amount) AS max_amount
FROM logistics_demos_catalog.og_refining_finance.financial_transactions_metrics
GROUP BY ALL
ORDER BY transaction_date
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `transaction_date`, MEASURE(`max_amount`) AS `max_transaction_amount`
FROM logistics_demos_catalog.og_refining_finance.financial_transactions_metrics
GROUP BY ALL
ORDER BY `transaction_date`
```

### oil_gas_refining/working_capital_cash_flow_optimization — BAD

**Q:** Which segments have the highest total free cash flow?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT segment, SUM(free_cash_flow_usd) AS total_metric
FROM logistics_demos_catalog.og_refining_cashflow.cashflow_monthly
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_segments AS (
  SELECT 
    segment, 
    SUM(free_cash_flow_usd) AS total_free_cash_flow_usd,
    RANK() OVER (ORDER BY SUM(free_cash_flow_usd) DESC) AS rnk
  FROM logistics_demos_catalog.og_refining_cashflow.cashflow_monthly
  WHERE segment IS NOT NULL
  GROUP BY segment
)
SELECT segment, total_free_cash_flow_usd
FROM ranked_segments
WHERE rnk = 1
```

### oil_gas_refining/working_capital_cash_flow_optimization — BAD

**Q:** Which business units have the most cash flow amount?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT business_unit_name, SUM(amount_usd) AS total_amount_usd
FROM logistics_demos_catalog.og_refining_cashflow.cash_transactions
GROUP BY business_unit_name
ORDER BY total_amount_usd DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_units AS (
  SELECT 
    `business_unit_name`, 
    SUM(`amount_usd`) AS `total_amount_usd`,
    RANK() OVER (ORDER BY SUM(`amount_usd`) DESC) AS `cashflow_rank`
  FROM `logistics_demos_catalog`.`og_refining_cashflow`.`cash_transactions`
  WHERE `business_unit_name` IS NOT NULL
  GROUP BY `business_unit_name`
)
SELECT `business_unit_name`, `total_amount_usd`
FROM ranked_units
WHERE `cashflow_rank` = 1
ORDER BY `business_unit_name`
```

### oil_gas_refining/working_capital_cash_flow_optimization — BAD

**Q:** What has been the peak accounts_receivable each month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT snapshot_date, MEASURE(max_accounts_receivable) AS max_accounts_receivable
FROM logistics_demos_catalog.og_refining_cashflow.working_capital_snapshots_metrics
GROUP BY ALL
ORDER BY snapshot_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `snapshot_date`) AS `month`, MEASURE(`max_accounts_receivable`) AS `peak_accounts_receivable_usd`
FROM `logistics_demos_catalog`.`og_refining_cashflow`.`working_capital_snapshots_metrics`
GROUP BY DATE_TRUNC('month', `snapshot_date`)
ORDER BY `month`
```

### oil_gas_refining/working_capital_cash_flow_optimization — BAD

**Q:** How has total outflows trended over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT transaction_date, MEASURE(total_outflows_usd) AS total_outflows_usd
FROM logistics_demos_catalog.og_refining_cashflow.cash_transactions_metrics
GROUP BY ALL
ORDER BY transaction_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `transaction_date`) AS `month`, MEASURE(`total_outflows_usd`) AS `total_outflows_usd`
FROM logistics_demos_catalog.og_refining_cashflow.cash_transactions_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_refining/working_capital_cash_flow_optimization — BAD

**Q:** How has total inflows trended over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT transaction_date, MEASURE(total_inflows_usd) AS total_inflows_usd
FROM logistics_demos_catalog.og_refining_cashflow.cash_transactions_metrics
GROUP BY ALL
ORDER BY transaction_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `transaction_date`) AS `month`, MEASURE(`total_inflows_usd`) AS `total_inflows_usd`
FROM logistics_demos_catalog.og_refining_cashflow.cash_transactions_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_integrated/predictive_maintenance_asset_health — BAD

**Q:** Which equipment assets have the highest total raw sensor measurement value?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT asset_name, SUM(sensor_value) AS total_sensor_value
FROM logistics_demos_catalog.og_integrated_pred_maint.sensor_readings
GROUP BY asset_name
ORDER BY total_sensor_value DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `asset_id`, SUM(`sensor_value`) AS `total_sensor_value`
FROM `logistics_demos_catalog`.`og_integrated_pred_maint`.`sensor_readings`
WHERE `asset_id` IS NOT NULL
GROUP BY `asset_id`
ORDER BY `total_sensor_value` DESC
LIMIT 10
```

### oil_gas_integrated/production_monitoring_control_center — BAD

**Q:** Which wells have the highest total daily oil production in barrels?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT well_name, SUM(oil_bbl) AS total_oil_bbl
FROM logistics_demos_catalog.og_integrated_production.production_readings
GROUP BY well_name
ORDER BY total_oil_bbl DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_wells AS (
  SELECT 
    `well_id`, 
    MEASURE(`total_oil_bbl`) AS total_oil_bbl,
    RANK() OVER (ORDER BY MEASURE(`total_oil_bbl`) DESC) AS rnk
  FROM logistics_demos_catalog.og_integrated_production.production_readings_metrics
  GROUP BY `well_id`
)
SELECT well_id, total_oil_bbl
FROM ranked_wells
WHERE rnk <= 10
ORDER BY total_oil_bbl DESC
```

### oil_gas_integrated/production_monitoring_control_center — BAD

**Q:** How has highest duration hours changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT event_date, MEASURE(max_duration_hours) AS max_duration_hours
FROM logistics_demos_catalog.og_integrated_production.production_events_metrics
GROUP BY ALL
ORDER BY event_date
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `event_date`, MEASURE(`max_duration_hours`) AS `max_duration_hours`
FROM `logistics_demos_catalog`.`og_integrated_production`.`production_events_metrics`
GROUP BY ALL
ORDER BY `event_date`
```

### oil_gas_integrated/production_monitoring_control_center — BAD

**Q:** How many distinct transactions appeared each month?

**Reasons:**
- EMPTY_RESULT

**Expected SQL:**
```sql
SELECT DATE_TRUNC('month', event_date) AS month, COUNT(DISTINCT event_id) AS distinct_events
FROM logistics_demos_catalog.og_integrated_production.production_events
WHERE event_id IS NOT NULL AND event_date IS NOT NULL
GROUP BY month
ORDER BY month; -- For readings (if user meant readings): -- SELECT DATE_TRUNC('month', reading_date) AS month, COUNT(DISTINCT reading_id) AS distinct_readings --
FROM logistics_demos_catalog.og_integrated_production.production_readings --
WHERE reading_id IS NOT NULL AND reading_date IS NOT NULL --
GROUP BY month --
ORDER BY month
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `event_date`) AS `month`, COUNT(DISTINCT `event_id`) AS `distinct_transactions`
FROM `logistics_demos_catalog`.`og_integrated_production`.`production_events`
WHERE `event_id` IS NOT NULL AND `event_date` IS NOT NULL
GROUP BY `month`
ORDER BY `month`
```

### oil_gas_integrated/production_monitoring_control_center — BAD

**Q:** Which wells have the highest total daily gas production in mcf?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT well_name, SUM(gas_mcf) AS total_metric
FROM logistics_demos_catalog.og_integrated_production.production_readings
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_wells AS (
  SELECT 
    `well_id`, 
    MEASURE(`total_gas_mcf`) AS `total_gas_mcf`,
    RANK() OVER (ORDER BY MEASURE(`total_gas_mcf`) DESC) AS rnk
  FROM logistics_demos_catalog.og_integrated_production.production_readings_metrics
  GROUP BY ALL
)
SELECT well_id, total_gas_mcf
FROM ranked_wells
WHERE rnk <= 10
ORDER BY total_gas_mcf DESC
```

### oil_gas_integrated/production_monitoring_control_center — BAD

**Q:** What has been the peak daily oil production in barrels each month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT reading_date, MEASURE(max_oil_bbl) AS max_oil_bbl
FROM logistics_demos_catalog.og_integrated_production.production_readings_metrics
GROUP BY ALL
ORDER BY reading_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `reading_date`) AS `month`, MEASURE(`max_oil_bbl`) AS `peak_daily_oil_bbl`
FROM logistics_demos_catalog.og_integrated_production.production_readings_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_integrated/capital_investment_simulation — BAD

**Q:** Which project type: deepwater, onshore conventional, shale, lng, infrastructures have the highest total budgeted amount for this category?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT project_type, SUM(budget_amount_usd) AS total_metric
FROM logistics_demos_catalog.og_integrated_capinvest.investment_transactions
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_types AS (
  SELECT 
    `project_type`, 
    MEASURE(`total_budget_usd`) AS `total_budget_usd`,
    RANK() OVER (ORDER BY MEASURE(`total_budget_usd`) DESC) AS rnk
  FROM logistics_demos_catalog.og_integrated_capinvest.investment_transactions_metrics
  WHERE `project_type` IS NOT NULL
  GROUP BY ALL
)
SELECT `project_type`, `total_budget_usd`
FROM ranked_types
WHERE rnk = 1
```

### oil_gas_integrated/capital_investment_simulation — BAD

**Q:** How has average net present value trended by month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT snapshot_date, MEASURE(avg_npv_mm) AS avg_npv_mm
FROM logistics_demos_catalog.og_integrated_capinvest.project_snapshots_metrics
GROUP BY ALL
ORDER BY snapshot_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `snapshot_date`) AS month, MEASURE(`avg_npv_mm`) AS average_npv_mm
FROM logistics_demos_catalog.og_integrated_capinvest.project_snapshots_metrics
GROUP BY ALL
ORDER BY month
```

### oil_gas_integrated/capital_investment_simulation — BAD

**Q:** How has highest amount changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT transaction_date, MEASURE(max_amount) AS max_amount
FROM logistics_demos_catalog.og_integrated_capinvest.investment_transactions_metrics
GROUP BY ALL
ORDER BY transaction_date
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `transaction_date`, MEASURE(`max_amount`) AS `max_amount_usd`
FROM logistics_demos_catalog.og_integrated_capinvest.investment_transactions_metrics
GROUP BY ALL
ORDER BY `transaction_date`
```

### oil_gas_integrated/working_capital_cash_flow_optimization — BAD

**Q:** Which business units have the most cash flow amount?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT business_unit_name, SUM(amount_usd) AS total_amount_usd
FROM logistics_demos_catalog.og_integrated_cashflow.cash_transactions
GROUP BY business_unit_name
ORDER BY total_amount_usd DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT business_unit_name, SUM(amount_usd) AS total_amount_usd
FROM logistics_demos_catalog.og_integrated_cashflow.cash_transactions
WHERE business_unit_name IS NOT NULL
GROUP BY business_unit_name
ORDER BY total_amount_usd DESC
```

### oil_gas_integrated/working_capital_cash_flow_optimization — BAD

**Q:** What has been the peak accounts_receivable each month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT snapshot_date, MEASURE(max_accounts_receivable) AS max_accounts_receivable
FROM logistics_demos_catalog.og_integrated_cashflow.working_capital_snapshots_metrics
GROUP BY ALL
ORDER BY snapshot_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `snapshot_date`) AS `month`, MEASURE(`max_accounts_receivable`) AS `peak_accounts_receivable_usd`
FROM `logistics_demos_catalog`.`og_integrated_cashflow`.`working_capital_snapshots_metrics`
WHERE `snapshot_date` IS NOT NULL
GROUP BY ALL
HAVING peak_accounts_receivable_usd IS NOT NULL
ORDER BY `month`
```

### oil_gas_integrated/scenario_planning_business_simulation — BAD

**Q:** Which scenario categorys have the highest total probability-weighted npv?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT scenario_category, SUM(weighted_npv_mm) AS total_metric
FROM logistics_demos_catalog.og_integrated_scenario.scenario_kpi_monthly
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH category_totals AS (
  SELECT 
    `scenario_category`, 
    SUM(`weighted_npv_mm`) AS total_weighted_npv_mm
  FROM `logistics_demos_catalog`.`og_integrated_scenario`.`scenario_kpi_monthly`
  WHERE `scenario_category` IS NOT NULL
  GROUP BY `scenario_category`
)
SELECT 
  `scenario_category`, 
  total_weighted_npv_mm
FROM (
  SELECT *, RANK() OVER (ORDER BY total_weighted_npv_mm DESC) AS rnk
  FROM category_totals
) ranked
WHERE rnk = 1
```

### oil_gas_integrated/scenario_planning_business_simulation — BAD

**Q:** Which scenario have the highest total assumed wti oil price per barrel?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT scenario_name, SUM(oil_price_assumption) AS total_oil_price_assumption
FROM logistics_demos_catalog.og_integrated_scenario.simulation_runs
GROUP BY scenario_name
ORDER BY total_oil_price_assumption DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH scenario_totals AS (
  SELECT 
    `scenario_id`, 
    SUM(`oil_price_assumption`) AS total_oil_price_assumption,
    RANK() OVER (ORDER BY SUM(`oil_price_assumption`) DESC) AS rnk
  FROM `logistics_demos_catalog`.`og_integrated_scenario`.`simulation_runs`
  WHERE `scenario_id` IS NOT NULL
  GROUP BY `scenario_id`
)
SELECT `scenario_id`, total_oil_price_assumption
FROM scenario_totals
WHERE rnk = 1
```

### oil_gas_integrated/scenario_planning_business_simulation — BAD

**Q:** Which scenario have the best average assumed annual production growth rate?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT scenario_name, AVG(production_growth_pct) AS avg_metric
FROM logistics_demos_catalog.og_integrated_scenario.simulation_runs
GROUP BY 1
ORDER BY avg_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH scenario_growth AS (SELECT `scenario_id`, MEASURE(`avg_production_growth_pct`) AS `avg_production_growth_pct` FROM `logistics_demos_catalog`.`og_integrated_scenario`.`simulation_runs_metrics` WHERE `scenario_id` IS NOT NULL GROUP BY ALL), ranked_scenarios AS (SELECT `scenario_id`, `avg_production_growth_pct`, RANK() OVER (ORDER BY `avg_production_growth_pct` DESC) AS `growth_rank` FROM scenario_growth) SELECT `scenario_id`, `avg_production_growth_pct` FROM ranked_scenarios WHERE `growth_rank` = 1 ORDER BY `scenario_id`
```

### oil_gas_integrated/scenario_planning_business_simulation — BAD

**Q:** How has average projected revenue trended by month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT snapshot_date, MEASURE(avg_projected_revenue_mm) AS avg_projected_revenue_mm
FROM logistics_demos_catalog.og_integrated_scenario.outcome_snapshots_metrics
GROUP BY ALL
ORDER BY snapshot_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `snapshot_date`) AS `month`, MEASURE(`avg_projected_revenue_mm`) AS `average_projected_revenue_mm`
FROM `logistics_demos_catalog`.`og_integrated_scenario`.`outcome_snapshots_metrics`
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_integrated/scenario_planning_business_simulation — BAD

**Q:** How has highest oil price assumption changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT run_date, MEASURE(max_oil_price_assumption) AS max_oil_price_assumption
FROM logistics_demos_catalog.og_integrated_scenario.simulation_runs_metrics
GROUP BY ALL
ORDER BY run_date
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `run_date`, MEASURE(`max_oil_price_assumption`) AS `max_oil_price_assumption`
FROM logistics_demos_catalog.og_integrated_scenario.simulation_runs_metrics
GROUP BY ALL
ORDER BY `run_date`
```

### oil_gas_integrated/financial_analytics_reporting — BAD

**Q:** Which cost centers have the highest total transaction amount?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT cost_center_name, SUM(amount_usd) AS total_amount_usd
FROM logistics_demos_catalog.og_integrated_finance.financial_transactions
GROUP BY cost_center_name
ORDER BY total_amount_usd DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_cost_centers AS (
  SELECT 
    `cost_center_id`, 
    MEASURE(`total_amount_usd`) AS `total_transaction_amount`,
    RANK() OVER (ORDER BY MEASURE(`total_amount_usd`) DESC) AS `rank`
  FROM logistics_demos_catalog.og_integrated_finance.financial_transactions_metrics
  WHERE `cost_center_id` IS NOT NULL
  GROUP BY ALL
)
SELECT `cost_center_id`, `total_transaction_amount`, `rank`
FROM ranked_cost_centers
WHERE `rank` <= 10
ORDER BY `rank`
```

### oil_gas_integrated/financial_analytics_reporting — BAD

**Q:** How has highest amount changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT transaction_date, MEASURE(max_amount) AS max_amount
FROM logistics_demos_catalog.og_integrated_finance.financial_transactions_metrics
GROUP BY ALL
ORDER BY transaction_date
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `transaction_date`, MEASURE(`max_amount`) AS `max_amount`
FROM `logistics_demos_catalog`.`og_integrated_finance`.`financial_transactions_metrics`
GROUP BY ALL
ORDER BY `transaction_date`
```
