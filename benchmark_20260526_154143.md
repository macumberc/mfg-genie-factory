# Genie Benchmark Results — benchmark_20260526_154143.jsonl

## Overall

- Total questions: **259**
- GOOD: **212** (81.9%)
- NEEDS_REVIEW: **0** (0.0%)
- BAD: **47** (18.1%)

## By subindustry

| Subindustry | Total | GOOD | NEEDS_REVIEW | BAD | Pass% |
|---|---:|---:|---:|---:|---:|
| logistics | 21 | 14 | 0 | 7 | 66.7% |
| machinery | 77 | 62 | 0 | 15 | 80.5% |
| oil_gas_integrated | 42 | 36 | 0 | 6 | 85.7% |
| oil_gas_midstream | 56 | 49 | 0 | 7 | 87.5% |
| oil_gas_refining | 42 | 33 | 0 | 9 | 78.6% |
| oil_gas_upstream | 21 | 18 | 0 | 3 | 85.7% |

## By spec

| Subindustry | Use case | Total | GOOD | NEEDS_REVIEW | BAD | Pass% |
|---|---|---:|---:|---:|---:|---:|
| logistics | fleet_planning_and_optimization | 7 | 5 | 0 | 2 | 71.4% |
| logistics | load_demand_forecasting | 7 | 5 | 0 | 2 | 71.4% |
| logistics | route_planning | 7 | 4 | 0 | 3 | 57.1% |
| machinery | asset_health | 7 | 5 | 0 | 2 | 71.4% |
| machinery | demand_forecasting | 7 | 6 | 0 | 1 | 85.7% |
| machinery | field_service_assistant | 7 | 6 | 0 | 1 | 85.7% |
| machinery | financial_analytics_reporting | 7 | 7 | 0 | 0 | 100.0% |
| machinery | machining_process_defect_detection | 7 | 5 | 0 | 2 | 71.4% |
| machinery | manufacturing_resource_planning | 7 | 5 | 0 | 2 | 71.4% |
| machinery | production_monitoring | 7 | 6 | 0 | 1 | 85.7% |
| machinery | quality_event_root_cause_analysis | 7 | 7 | 0 | 0 | 100.0% |
| machinery | spare_part_inventory_optimization | 7 | 4 | 0 | 3 | 57.1% |
| machinery | spend_intelligence | 7 | 7 | 0 | 0 | 100.0% |
| machinery | working_capital_cash_flow_optimization | 7 | 4 | 0 | 3 | 57.1% |
| oil_gas_integrated | capital_investment_simulation | 7 | 6 | 0 | 1 | 85.7% |
| oil_gas_integrated | financial_analytics_reporting | 7 | 5 | 0 | 2 | 71.4% |
| oil_gas_integrated | predictive_maintenance_asset_health | 7 | 7 | 0 | 0 | 100.0% |
| oil_gas_integrated | production_monitoring_control_center | 7 | 5 | 0 | 2 | 71.4% |
| oil_gas_integrated | scenario_planning_business_simulation | 7 | 7 | 0 | 0 | 100.0% |
| oil_gas_integrated | working_capital_cash_flow_optimization | 7 | 6 | 0 | 1 | 85.7% |
| oil_gas_midstream | automated_reporting_of_carbon_intensity | 7 | 7 | 0 | 0 | 100.0% |
| oil_gas_midstream | energy_trading | 7 | 7 | 0 | 0 | 100.0% |
| oil_gas_midstream | financial_analytics_reporting | 7 | 5 | 0 | 2 | 71.4% |
| oil_gas_midstream | logistics_optimization | 7 | 6 | 0 | 1 | 85.7% |
| oil_gas_midstream | regulation_compliance | 7 | 7 | 0 | 0 | 100.0% |
| oil_gas_midstream | scenario_planning_business_simulation | 7 | 6 | 0 | 1 | 85.7% |
| oil_gas_midstream | spend_intelligence | 7 | 6 | 0 | 1 | 85.7% |
| oil_gas_midstream | working_capital_cash_flow_optimization | 7 | 5 | 0 | 2 | 71.4% |
| oil_gas_refining | energy_use_monitoring_heat | 7 | 5 | 0 | 2 | 71.4% |
| oil_gas_refining | financial_analytics_reporting | 7 | 5 | 0 | 2 | 71.4% |
| oil_gas_refining | predictive_maintenance_asset_health | 7 | 6 | 0 | 1 | 85.7% |
| oil_gas_refining | production_monitoring | 7 | 5 | 0 | 2 | 71.4% |
| oil_gas_refining | quality_event_root_cause_analysis | 7 | 7 | 0 | 0 | 100.0% |
| oil_gas_refining | working_capital_cash_flow_optimization | 7 | 5 | 0 | 2 | 71.4% |
| oil_gas_upstream | predictive_maintenance_asset_health | 7 | 5 | 0 | 2 | 71.4% |
| oil_gas_upstream | reservoir_management | 7 | 7 | 0 | 0 | 100.0% |
| oil_gas_upstream | well_production_monitoring_flow | 7 | 6 | 0 | 1 | 85.7% |

## Failing / needs-review questions (47)

### machinery/field_service_assistant — BAD

**Q:** Which equipment models have the highest average first-time fix rate?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
WITH ranked_models AS ( SELECT equipment_model, AVG(first_time_fix_rate_pct) AS avg_first_time_fix_rate_pct, RANK() OVER (ORDER BY AVG(first_time_fix_rate_pct) DESC) AS rank
FROM logistics_demos_catalog.mach_field_service.service_kpi_monthly
WHERE equipment_model IS NOT NULL AND first_time_fix_rate_pct IS NOT NULL
GROUP BY equipment_model ) SELECT equipment_model, avg_first_time_fix_rate_pct
FROM ranked_models
WHERE rank <= 10
ORDER BY avg_first_time_fix_rate_pct DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_models AS (
  SELECT 
    `equipment_model`, 
    AVG(`first_time_fix_rate_pct`) AS `avg_first_time_fix_rate_pct`,
    RANK() OVER (ORDER BY AVG(`first_time_fix_rate_pct`) DESC) AS rnk
  FROM logistics_demos_catalog.mach_field_service.service_kpi_monthly
  WHERE `equipment_model` IS NOT NULL AND `first_time_fix_rate_pct` IS NOT NULL
  GROUP BY `equipment_model`
)
SELECT `equipment_model`, `avg_first_time_fix_rate_pct`
FROM ranked_models
WHERE rnk = 1
```

### logistics/load_demand_forecasting — BAD

**Q:** Which lane categorys have the highest number of forecast records?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
WITH category_counts AS (SELECT lane_category, MEASURE(forecast_record_count) AS forecast_record_count
FROM logistics_demos_catalog.load_forecasting.demand_forecasts_metrics
WHERE lane_category IS NOT NULL
GROUP BY ALL) SELECT lane_category, forecast_record_count
FROM (SELECT lane_category, forecast_record_count, RANK() OVER (ORDER BY forecast_record_count DESC) AS rank
FROM category_counts)
WHERE rank = 1
```

**Generated SQL:**
```sql
SELECT `lane_category`, MEASURE(`forecast_record_count`) AS `forecast_record_count`, RANK() OVER (ORDER BY MEASURE(`forecast_record_count`) DESC) AS `rank`
FROM `logistics_demos_catalog`.`load_forecasting`.`demand_forecasts_metrics`
WHERE `lane_category` IS NOT NULL
GROUP BY ALL
ORDER BY `forecast_record_count` DESC
```

### logistics/load_demand_forecasting — BAD

**Q:** How has total weight delivered lbs count changed over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT DATE_TRUNC('month', order_date) AS month, MEASURE(total_weight_delivered_lbs) AS total_weight_delivered_lbs
FROM logistics_demos_catalog.load_forecasting.shipment_orders_metrics
WHERE order_date IS NOT NULL
GROUP BY ALL
ORDER BY month
```

**Generated SQL:**
```sql
SELECT `order_date`, MEASURE(`total_weight_delivered_lbs`) AS `total_weight_delivered_lbs`
FROM `logistics_demos_catalog`.`load_forecasting`.`shipment_orders_metrics`
WHERE `order_date` IS NOT NULL
GROUP BY ALL
ORDER BY `order_date`
```

### logistics/route_planning — BAD

**Q:** Which vehicles have the best load weight of vehicle capacity?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
WITH ranked_vehicles AS ( SELECT vehicle_type, MEASURE(avg_load_utilization_pct) AS average_load_utilization_pct, RANK() OVER (ORDER BY MEASURE(avg_load_utilization_pct) DESC) AS utilization_rank
FROM logistics_demos_catalog.route_planning.route_segments_metrics
WHERE vehicle_type IS NOT NULL
GROUP BY ALL ) SELECT vehicle_type, average_load_utilization_pct
FROM ranked_vehicles
WHERE utilization_rank <= 10
ORDER BY utilization_rank
```

**Generated SQL:**
```sql
SELECT `vehicle_id`, `vehicle_name`, AVG(`load_utilization_pct`) AS `avg_load_utilization_pct`
FROM `logistics_demos_catalog`.`route_planning`.`route_segments`
WHERE `vehicle_id` IS NOT NULL AND `vehicle_name` IS NOT NULL AND `load_utilization_pct` IS NOT NULL
GROUP BY `vehicle_id`, `vehicle_name`
ORDER BY `avg_load_utilization_pct` DESC
```

### logistics/route_planning — BAD

**Q:** Which vehicles have the highest total weekly distance traveled?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
WITH vehicle_distances AS ( SELECT vehicle_id, vehicle_type, MEASURE(total_weekly_distance_km) AS total_distance
FROM logistics_demos_catalog.route_planning.route_efficiency_metrics_mv
GROUP BY ALL ) SELECT vehicle_id, vehicle_type, total_distance, RANK() OVER (ORDER BY total_distance DESC) AS distance_rank
FROM vehicle_distances
WHERE total_distance IS NOT NULL QUALIFY distance_rank <= 10
ORDER BY distance_rank, vehicle_id
```

**Generated SQL:**
```sql
WITH vehicle_distance AS (
  SELECT 
    `vehicle_id`, 
    MEASURE(`total_weekly_distance_km`) AS total_distance_km
  FROM logistics_demos_catalog.route_planning.route_efficiency_metrics_mv
  GROUP BY `vehicle_id`
)
SELECT 
  `vehicle_id`, 
  total_distance_km
FROM (
  SELECT *, RANK() OVER (ORDER BY total_distance_km DESC) as rnk
  FROM vehicle_distance
) ranked
WHERE rnk <= 10
ORDER BY total_distance_km DESC
```

### logistics/route_planning — BAD

**Q:** Which depots have driven the most kilometers in the past week?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
WITH latest_week AS (SELECT MAX(week_start) AS week_start
FROM logistics_demos_catalog.route_planning.route_efficiency_metrics_mv), vehicle_distance AS ( SELECT vehicle_id, MEASURE(total_weekly_distance_km) AS total_distance_km
FROM logistics_demos_catalog.route_planning.route_efficiency_metrics_mv
WHERE week_start = (SELECT week_start
FROM latest_week)
GROUP BY ALL ) SELECT v.home_depot AS depot, SUM(vd.total_distance_km) AS total_distance_km
FROM vehicle_distance vd
JOIN logistics_demos_catalog.route_planning.vehicles v ON vd.vehicle_id = v.vehicle_id
WHERE v.home_depot IS NOT NULL
GROUP BY v.home_depot
ORDER BY total_distance_km DESC
```

**Generated SQL:**
```sql
SELECT v.`home_depot`, SUM(rs.`distance_km`) AS `total_distance_km`
FROM `logistics_demos_catalog`.`route_planning`.`route_segments` rs
JOIN `logistics_demos_catalog`.`route_planning`.`vehicles` v ON rs.`vehicle_id` = v.`vehicle_id`
WHERE rs.`segment_date` BETWEEN DATE_SUB(CURRENT_DATE, 6) AND CURRENT_DATE
  AND v.`home_depot` IS NOT NULL
  AND rs.`distance_km` IS NOT NULL
GROUP BY v.`home_depot`
ORDER BY `total_distance_km` DESC;
```

### machinery/production_monitoring — BAD

**Q:** Which production line types have the most planned production quantity?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT line_type, SUM(planned_qty) AS total_planned_qty
FROM logistics_demos_catalog.mach_production_mon.production_runs
GROUP BY line_type
ORDER BY total_planned_qty DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_line_types AS (
  SELECT
    `line_type`,
    MEASURE(`total_planned_qty`) AS `total_planned_qty`,
    RANK() OVER (ORDER BY MEASURE(`total_planned_qty`) DESC) AS `qty_rank`
  FROM `logistics_demos_catalog`.`mach_production_mon`.`production_runs_metrics`
  WHERE `line_type` IS NOT NULL
  GROUP BY ALL
)
SELECT `line_type`, `total_planned_qty`
FROM ranked_line_types
WHERE `qty_rank` = 1
ORDER BY `line_type`
```

### machinery/asset_health — BAD

**Q:** Which type of industrial assets have the highest average asset availability percentage?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
WITH asset_type_availability AS (SELECT asset_type, MEASURE(avg_availability) AS avg_availability
FROM logistics_demos_catalog.mach_asset_health.asset_health_monthly_metrics
WHERE asset_type IS NOT NULL
GROUP BY ALL), ranked_types AS (SELECT asset_type, avg_availability, RANK() OVER (ORDER BY avg_availability DESC) AS rk
FROM asset_type_availability) SELECT asset_type, avg_availability
FROM ranked_types
WHERE rk = 1
```

**Generated SQL:**
```sql
SELECT `asset_type`, MEASURE(`avg_availability`) AS `avg_availability`
FROM `logistics_demos_catalog`.`mach_asset_health`.`asset_health_monthly_metrics`
WHERE `asset_type` IS NOT NULL
GROUP BY ALL
HAVING avg_availability IS NOT NULL
ORDER BY `avg_availability` DESC
```

### machinery/asset_health — BAD

**Q:** Which type of industrial assets have the highest average bearing temperature in celsius?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
WITH avg_temp_by_type AS (SELECT asset_type, MEASURE(avg_bearing_temp) AS avg_bearing_temp
FROM logistics_demos_catalog.mach_asset_health.condition_readings_metrics
WHERE asset_type IS NOT NULL
GROUP BY ALL) SELECT asset_type, avg_bearing_temp
FROM (SELECT asset_type, avg_bearing_temp, RANK() OVER (ORDER BY avg_bearing_temp DESC) AS rank
FROM avg_temp_by_type)
WHERE rank = 1
```

**Generated SQL:**
```sql
SELECT `asset_type`, MEASURE(`avg_bearing_temp`) AS `avg_bearing_temp`
FROM `logistics_demos_catalog`.`mach_asset_health`.`condition_readings_metrics`
GROUP BY ALL
ORDER BY `avg_bearing_temp` DESC
```

### logistics/fleet_planning_and_optimization — BAD

**Q:** Which vehicles have the best dispatched hours of available hours?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
WITH vehicle_utilization AS ( SELECT vehicle_id, vehicle_name, AVG(utilization_pct) AS avg_utilization_pct
FROM logistics_demos_catalog.fleet_planning.vehicle_utilization_snapshots
WHERE vehicle_id IS NOT NULL AND utilization_pct IS NOT NULL
GROUP BY vehicle_id, vehicle_name ) SELECT vehicle_id, vehicle_name, avg_utilization_pct, RANK() OVER (ORDER BY avg_utilization_pct DESC) AS utilization_rank
FROM vehicle_utilization
ORDER BY utilization_rank ASC, vehicle_id
```

**Generated SQL:**
```sql
WITH ranked_vehicles AS (
  SELECT 
    `vehicle_id`,
    `vehicle_name`,
    AVG(`utilization_pct`) AS `avg_utilization_pct`,
    RANK() OVER (ORDER BY AVG(`utilization_pct`) DESC) AS `utilization_rank`
  FROM `logistics_demos_catalog`.`fleet_planning`.`vehicle_utilization_snapshots`
  WHERE `utilization_pct` IS NOT NULL AND `vehicle_name` IS NOT NULL
  GROUP BY `vehicle_id`, `vehicle_name`
)
SELECT 
  `vehicle_id`,
  `vehicle_name`,
  `avg_utilization_pct`
FROM ranked_vehicles
WHERE `utilization_rank` <= 10
ORDER BY `utilization_rank`
```

### logistics/fleet_planning_and_optimization — BAD

**Q:** Which vehicle class: heavy duty, medium duty, light duty, sprinters have the highest total cargo weight in kilograms?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_MISSING_OR_INCORRECT_FILTER

**Expected SQL:**
```sql
SELECT vehicle_class, MEASURE(total_cargo_weight_kg) AS total_cargo_weight_kg
FROM logistics_demos_catalog.fleet_planning.fleet_operations_metrics
WHERE vehicle_class IS NOT NULL
GROUP BY ALL
ORDER BY total_cargo_weight_kg DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_classes AS (
  SELECT 
    `vehicle_class`, 
    MEASURE(`total_cargo_weight_kg`) AS `total_cargo_weight_kg`,
    RANK() OVER (ORDER BY MEASURE(`total_cargo_weight_kg`) DESC) AS rnk
  FROM logistics_demos_catalog.fleet_planning.fleet_operations_metrics
  GROUP BY ALL
)
SELECT `vehicle_class`, `total_cargo_weight_kg`
FROM ranked_classes
WHERE rnk = 1
```

### machinery/machining_process_defect_detection — BAD

**Q:** Which cnc machine models have the highest average defect rate percentage?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
WITH ranked_models AS ( SELECT machine_model, MEASURE(avg_defect_rate) AS avg_defect_rate, RANK() OVER (ORDER BY MEASURE(avg_defect_rate) DESC) AS defect_rate_rank
FROM logistics_demos_catalog.mach_machining_defect.defect_kpi_metrics
WHERE machine_model IS NOT NULL
GROUP BY ALL ) SELECT machine_model, avg_defect_rate
FROM ranked_models
WHERE defect_rate_rank = 1
```

**Generated SQL:**
```sql
SELECT `machine_model`, MEASURE(`avg_defect_rate`) AS `avg_defect_rate`
FROM `logistics_demos_catalog`.`mach_machining_defect`.`defect_kpi_metrics`
WHERE `machine_model` IS NOT NULL
GROUP BY ALL
HAVING avg_defect_rate IS NOT NULL
ORDER BY `avg_defect_rate` DESC
```

### machinery/machining_process_defect_detection — BAD

**Q:** Which cnc machine models have the highest total surface roughness ra in micrometers?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_METRIC_CALCULATION

**Expected SQL:**
```sql
SELECT machine_model, MEASURE(avg_surface_finish) AS total_surface_roughness_ra_um
FROM logistics_demos_catalog.mach_machining_defect.machining_events_metrics
WHERE machine_model IS NOT NULL
GROUP BY ALL
ORDER BY total_surface_roughness_ra_um DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `machine_model`, SUM(`surface_finish_um`) AS `total_surface_finish_um`
FROM `logistics_demos_catalog`.`mach_machining_defect`.`machining_events`
WHERE `machine_model` IS NOT NULL
GROUP BY `machine_model`
ORDER BY `total_surface_finish_um` DESC
```

### machinery/working_capital_cash_flow_optimization — BAD

**Q:** Which business unit types have the highest total inventory value?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT business_unit_type, SUM(inventory_value_usd) AS total_metric
FROM logistics_demos_catalog.mach_working_capital.working_capital_snapshots
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_types AS (
  SELECT 
    `business_unit_type`, 
    SUM(`inventory_value_usd`) AS total_inventory_value,
    RANK() OVER (ORDER BY SUM(`inventory_value_usd`) DESC) AS rnk
  FROM logistics_demos_catalog.mach_working_capital.working_capital_snapshots
  WHERE `business_unit_type` IS NOT NULL AND `inventory_value_usd` IS NOT NULL
  GROUP BY `business_unit_type`
)
SELECT `business_unit_type`, total_inventory_value
FROM ranked_types
WHERE rnk = 1
```

### machinery/working_capital_cash_flow_optimization — BAD

**Q:** Which business unit types have the highest total transaction amount?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
WITH ranked_bu_types AS ( SELECT business_unit_type, SUM(amount_usd) AS total_amount_usd, RANK() OVER (ORDER BY SUM(amount_usd) DESC) AS rnk
FROM logistics_demos_catalog.mach_working_capital.cash_flow_transactions
WHERE business_unit_type IS NOT NULL
GROUP BY business_unit_type ) SELECT business_unit_type, total_amount_usd
FROM ranked_bu_types
WHERE rnk = 1
```

**Generated SQL:**
```sql
SELECT 
  `business_unit_type`, 
  MEASURE(`total_inflow`) + MEASURE(`total_outflow`) AS `total_transaction_amount`
FROM logistics_demos_catalog.mach_working_capital.cash_flow_txn_metrics
GROUP BY ALL
ORDER BY `total_transaction_amount` DESC
```

### machinery/working_capital_cash_flow_optimization — BAD

**Q:** How has total outflow count changed over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT txn_date, MEASURE(total_transactions) AS total_outflow_count
FROM logistics_demos_catalog.mach_working_capital.cash_flow_txn_metrics
WHERE direction = 'Outflow'
GROUP BY ALL
ORDER BY txn_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `txn_date`) AS `month`, MEASURE(`total_transactions`) AS `outflow_transaction_count`
FROM `logistics_demos_catalog`.`mach_working_capital`.`cash_flow_txn_metrics`
WHERE `direction` = 'Outflow' AND `txn_date` IS NOT NULL
GROUP BY ALL
ORDER BY `month`
```

### machinery/manufacturing_resource_planning — BAD

**Q:** Which materials have the highest shortage hours this month?

**Reasons:**
- EMPTY_RESULT

**Expected SQL:**
```sql
SELECT material_name, SUM(shortage_hours) AS total_shortage_hours
FROM logistics_demos_catalog.mach_mrp.material_requirements
WHERE DATE_TRUNC('month', requirement_date) = DATE_TRUNC('month', CURRENT_DATE())
GROUP BY material_name
ORDER BY total_shortage_hours DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH shortage_hours_cte AS (SELECT work_center_id, SUM(COALESCE(planned_hours, 0) - COALESCE(actual_hours, 0)) AS shortage_hours FROM logistics_demos_catalog.mach_mrp.work_orders WHERE wo_date >= DATE_TRUNC('month', CURRENT_DATE) AND wo_date < DATE_TRUNC('month', CURRENT_DATE + INTERVAL 1 MONTH) GROUP BY work_center_id) SELECT work_center_id, shortage_hours FROM shortage_hours_cte ORDER BY shortage_hours DESC LIMIT 10
```

### machinery/manufacturing_resource_planning — BAD

**Q:** How many distinct forecasts appeared each month?

**Reasons:**
- EMPTY_RESULT

**Expected SQL:**
```sql
SELECT DATE_TRUNC('month', kpi_month) AS month, COUNT(DISTINCT forecast) AS distinct_count
FROM logistics_demos_catalog.mach_mrp.planning_kpi_monthly
GROUP BY 1
ORDER BY 1
```

**Generated SQL:**
```sql
There is no information about forecasts in the available tables, so this question cannot be answered with the current data. If you can clarify what you mean by "forecasts" or specify which table or column contains forecast data, I can assist further.
```

### machinery/demand_forecasting — BAD

**Q:** What has been the peak forecast_accuracy_percent each month?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_FORMATTING_ERROR

**Expected SQL:**
```sql
SELECT kpi_month, MEASURE(max_forecast_accuracy_percent) AS max_forecast_accuracy_percent
FROM logistics_demos_catalog.mach_demand_forecast.demand_kpi_metrics
GROUP BY ALL
ORDER BY kpi_month
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `kpi_month`) AS `month`, MEASURE(`max_forecast_accuracy_percent`) AS `peak_forecast_accuracy_percent`
FROM `logistics_demos_catalog`.`mach_demand_forecast`.`demand_kpi_metrics`
GROUP BY ALL
ORDER BY `month`
LIMIT 10
```

### machinery/spare_part_inventory_optimization — BAD

**Q:** Which spare part numbers have the highest average inventory turnover ratio?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT part_number, AVG(turnover_ratio) AS avg_turnover_ratio, RANK() OVER (ORDER BY AVG(turnover_ratio) DESC) AS turnover_rank
FROM logistics_demos_catalog.mach_spare_parts.inventory_kpi_monthly
WHERE part_number IS NOT NULL AND turnover_ratio IS NOT NULL
GROUP BY part_number
ORDER BY avg_turnover_ratio DESC
```

**Generated SQL:**
```sql
WITH avg_turnover AS (
  SELECT 
    `part_number`, 
    AVG(`turnover_ratio`) AS `avg_turnover_ratio`
  FROM `logistics_demos_catalog`.`mach_spare_parts`.`inventory_kpi_monthly`
  WHERE `part_number` IS NOT NULL AND `turnover_ratio` IS NOT NULL
  GROUP BY `part_number`
)
SELECT 
  `part_number`, 
  `avg_turnover_ratio`
FROM (
  SELECT *, RANK() OVER (ORDER BY `avg_turnover_ratio` DESC) as rank
  FROM avg_turnover
) ranked
WHERE rank = 1
```

### machinery/spare_part_inventory_optimization — BAD

**Q:** What are the top spare part number by total transaction quantity?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
WITH ranked_parts AS ( SELECT part_number, SUM(quantity) AS total_quantity, RANK() OVER (ORDER BY SUM(quantity) DESC) AS quantity_rank
FROM logistics_demos_catalog.mach_spare_parts.parts_transactions
WHERE part_number IS NOT NULL
GROUP BY part_number ) SELECT part_number, total_quantity
FROM ranked_parts
WHERE quantity_rank <= 10
ORDER BY quantity_rank
```

**Generated SQL:**
```sql
WITH ranked_parts AS (
  SELECT
    `part_number`,
    SUM(`quantity`) AS `total_transaction_quantity`,
    RANK() OVER (ORDER BY SUM(`quantity`) DESC) AS `quantity_rank`
  FROM `logistics_demos_catalog`.`mach_spare_parts`.`parts_transactions`
  WHERE `part_number` IS NOT NULL
  GROUP BY `part_number`
)
SELECT `part_number`, `total_transaction_quantity`
FROM ranked_parts
WHERE `quantity_rank` = 1
ORDER BY `part_number`;
```

### machinery/spare_part_inventory_optimization — BAD

**Q:** How has total received qty count changed over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT DATE_TRUNC('month', txn_date) AS month, MEASURE(total_received_qty) AS total_received_qty
FROM logistics_demos_catalog.mach_spare_parts.parts_transactions_metrics
WHERE txn_date IS NOT NULL
GROUP BY ALL
ORDER BY month
```

**Generated SQL:**
```sql
SELECT `txn_date`, MEASURE(`total_received_qty`) AS `total_received_qty`
FROM `logistics_demos_catalog`.`mach_spare_parts`.`parts_transactions_metrics`
GROUP BY ALL
ORDER BY `txn_date`
```

### oil_gas_upstream/predictive_maintenance_asset_health — BAD

**Q:** How has highest downtime hours changed over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT event_date, MAX(downtime_hours) AS max_downtime_hours
FROM logistics_demos_catalog.og_upstream_pred_maint.maintenance_events
GROUP BY event_date
ORDER BY event_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `event_date`) AS `month`, MEASURE(`max_downtime_hours`) AS `max_downtime_hours`
FROM logistics_demos_catalog.og_upstream_pred_maint.maintenance_events_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_upstream/predictive_maintenance_asset_health — BAD

**Q:** Which equipments have the highest total sensor value?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT equipment_id, MEASURE(total_sensor_value) AS total_sensor_value
FROM logistics_demos_catalog.og_upstream_pred_maint.sensor_readings_metrics
WHERE equipment_id IS NOT NULL
GROUP BY ALL
ORDER BY total_sensor_value DESC
```

**Generated SQL:**
```sql
WITH ranked_equipment AS (
  SELECT 
    `equipment_id`, 
    MEASURE(`total_sensor_value`) AS `total_sensor_value`,
    RANK() OVER (ORDER BY MEASURE(`total_sensor_value`) DESC) AS `rank`
  FROM logistics_demos_catalog.og_upstream_pred_maint.sensor_readings_metrics
  GROUP BY ALL
)
SELECT `equipment_id`, `total_sensor_value`
FROM ranked_equipment
WHERE `rank` <= 10
ORDER BY `total_sensor_value` DESC
```

### oil_gas_upstream/well_production_monitoring_flow — BAD

**Q:** Which wells have the highest average oil barrels per day?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_MISSING_OR_INCORRECT_FILTER

**Expected SQL:**
```sql
WITH ranked_wells AS ( SELECT well_id, MEASURE(avg_oil_bpd) AS avg_oil_bpd, RANK() OVER (ORDER BY MEASURE(avg_oil_bpd) DESC) AS rank
FROM logistics_demos_catalog.og_upstream_wellflow.production_readings_metrics
GROUP BY ALL ) SELECT well_id, avg_oil_bpd
FROM ranked_wells
WHERE rank <= 10
ORDER BY avg_oil_bpd DESC
```

**Generated SQL:**
```sql
SELECT `well_id`, MEASURE(`avg_oil_bpd`) AS `avg_oil_bpd`, RANK() OVER (ORDER BY MEASURE(`avg_oil_bpd`) DESC) AS `oil_rank`
FROM `logistics_demos_catalog`.`og_upstream_wellflow`.`production_readings_metrics`
GROUP BY ALL
ORDER BY `avg_oil_bpd` DESC
```

### oil_gas_midstream/scenario_planning_business_simulation — BAD

**Q:** Which scenarios have the best average throughput change percentage?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
WITH scenario_throughput AS (SELECT scenario_id, MEASURE(avg_throughput_change_pct) AS avg_throughput_change_pct
FROM logistics_demos_catalog.og_midstream_scenario.simulation_runs_metrics
WHERE scenario_id IS NOT NULL
GROUP BY ALL) SELECT scenario_id, avg_throughput_change_pct, RANK() OVER (ORDER BY avg_throughput_change_pct DESC) AS rank
FROM scenario_throughput
WHERE avg_throughput_change_pct IS NOT NULL QUALIFY rank <= 10
ORDER BY rank
```

**Generated SQL:**
```sql
SELECT `scenario_id`, MEASURE(`avg_throughput_change_pct`) AS `avg_throughput_change_pct`
FROM logistics_demos_catalog.og_midstream_scenario.simulation_runs_metrics
WHERE `scenario_id` IS NOT NULL
GROUP BY ALL
ORDER BY `avg_throughput_change_pct` DESC
```

### oil_gas_midstream/logistics_optimization — BAD

**Q:** How has total volume impact barrels trended over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT DATE_TRUNC('month', event_date) AS month, MEASURE(total_volume_impact_bbl) AS total_volume_impact_bbl
FROM logistics_demos_catalog.og_midstream_logistics.logistics_events_metrics
GROUP BY ALL
ORDER BY month
```

**Generated SQL:**
```sql
SELECT event_date, SUM(volume_impact_bbl) AS total_volume_impact_bbl
FROM logistics_demos_catalog.og_midstream_logistics.logistics_events
GROUP BY event_date
ORDER BY event_date
```

### oil_gas_midstream/financial_analytics_reporting — BAD

**Q:** Which cost centers have the highest total actual revenue?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT cost_center_id, MEASURE(total_actual_revenue) AS total_actual_revenue
FROM logistics_demos_catalog.og_midstream_finance.budget_snapshots_metrics
WHERE cost_center_id IS NOT NULL
GROUP BY ALL
ORDER BY total_actual_revenue DESC
```

**Generated SQL:**
```sql
WITH ranked_cost_centers AS (
  SELECT 
    `cost_center_id`, 
    MEASURE(`total_actual_revenue`) AS `total_actual_revenue`,
    RANK() OVER (ORDER BY MEASURE(`total_actual_revenue`) DESC) AS revenue_rank
  FROM logistics_demos_catalog.og_midstream_finance.budget_snapshots_metrics
  WHERE `cost_center_id` IS NOT NULL
  GROUP BY ALL
)
SELECT `cost_center_id`, `total_actual_revenue`
FROM ranked_cost_centers
WHERE revenue_rank <= 10
ORDER BY `total_actual_revenue` DESC
```

### oil_gas_midstream/financial_analytics_reporting — BAD

**Q:** Which cost centers have the highest total transaction amount?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_FILTER

**Expected SQL:**
```sql
WITH ranked_cost_centers AS ( SELECT cost_center_id, MEASURE(total_amount_usd) AS total_transaction_amount, RANK() OVER (ORDER BY MEASURE(total_amount_usd) DESC) AS rank
FROM logistics_demos_catalog.og_midstream_finance.financial_transactions_metrics
WHERE cost_center_id IS NOT NULL
GROUP BY ALL ) SELECT cost_center_id, total_transaction_amount, rank
FROM ranked_cost_centers
WHERE rank = 1
ORDER BY cost_center_id
```

**Generated SQL:**
```sql
WITH ranked_cost_centers AS (
  SELECT 
    `cost_center_id`, 
    MEASURE(`total_amount_usd`) AS `total_transaction_amount`,
    RANK() OVER (ORDER BY MEASURE(`total_amount_usd`) DESC) AS rnk
  FROM logistics_demos_catalog.og_midstream_finance.financial_transactions_metrics
  GROUP BY ALL
)
SELECT `cost_center_id`, `total_transaction_amount`
FROM ranked_cost_centers
WHERE rnk <= 10
ORDER BY `total_transaction_amount` DESC
```

### oil_gas_midstream/working_capital_cash_flow_optimization — BAD

**Q:** Which segments have the highest total free cash flow?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT segment, SUM(free_cash_flow_usd) AS total_metric
FROM logistics_demos_catalog.og_midstream_cashflow.cashflow_monthly
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_segments AS (
  SELECT 
    `segment`, 
    SUM(`free_cash_flow_usd`) AS `total_free_cash_flow_usd`,
    RANK() OVER (ORDER BY SUM(`free_cash_flow_usd`) DESC) AS `rank`
  FROM logistics_demos_catalog.og_midstream_cashflow.cashflow_monthly
  WHERE `segment` IS NOT NULL AND `free_cash_flow_usd` IS NOT NULL
  GROUP BY `segment`
)
SELECT `segment`, `total_free_cash_flow_usd`
FROM ranked_segments
WHERE `rank` = 1
```

### oil_gas_midstream/working_capital_cash_flow_optimization — BAD

**Q:** Which business units have the highest average dso?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT business_unit_id, MEASURE(avg_dso_days) AS average_dso_days
FROM logistics_demos_catalog.og_midstream_cashflow.working_capital_snapshots_metrics
WHERE business_unit_id IS NOT NULL
GROUP BY ALL
HAVING average_dso_days IS NOT NULL
ORDER BY average_dso_days DESC
```

**Generated SQL:**
```sql
SELECT `business_unit_id`, MEASURE(`avg_dso_days`) AS `average_dso_days`
FROM `logistics_demos_catalog`.`og_midstream_cashflow`.`working_capital_snapshots_metrics`
WHERE `business_unit_id` IS NOT NULL
GROUP BY ALL
HAVING average_dso_days IS NOT NULL
ORDER BY `average_dso_days` DESC
LIMIT 10
```

### oil_gas_refining/production_monitoring — BAD

**Q:** How has average throughput barrels per day trended by month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT DATE_TRUNC('month', reading_date) AS month, MEASURE(avg_throughput_bpd) AS avg_throughput_bpd
FROM logistics_demos_catalog.og_refining_production.throughput_readings_metrics
GROUP BY ALL
ORDER BY month
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `snapshot_date`) AS `month`, AVG(`avg_throughput_bpd`) AS `avg_throughput_bpd`
FROM `logistics_demos_catalog`.`og_refining_production`.`production_snapshots`
WHERE `avg_throughput_bpd` IS NOT NULL AND `snapshot_date` IS NOT NULL
GROUP BY `month`
ORDER BY `month`
```

### oil_gas_refining/production_monitoring — BAD

**Q:** How has average throughput in barrels per day trended by month?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT DATE_TRUNC('month', reading_date) AS month, MEASURE(avg_throughput_bpd) AS avg_throughput_bpd
FROM logistics_demos_catalog.og_refining_production.throughput_readings_metrics
GROUP BY ALL
ORDER BY month
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `snapshot_date`) AS `month`, AVG(`avg_throughput_bpd`) AS `avg_throughput_bpd`
FROM `logistics_demos_catalog`.`og_refining_production`.`production_snapshots`
WHERE `avg_throughput_bpd` IS NOT NULL AND `snapshot_date` IS NOT NULL
GROUP BY `month`
ORDER BY `month`
```

### oil_gas_refining/predictive_maintenance_asset_health — BAD

**Q:** How has highest downtime hours changed over time?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT DATE_TRUNC('month', event_date) AS month, MEASURE(max_downtime_hours) AS max_downtime_hours
FROM logistics_demos_catalog.og_refining_pred_maint.maintenance_events_metrics
GROUP BY ALL
ORDER BY month
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('day', `event_date`) AS `event_day`, MEASURE(`max_downtime_hours`) AS `max_downtime_hours`
FROM logistics_demos_catalog.og_refining_pred_maint.maintenance_events_metrics
GROUP BY ALL
ORDER BY `event_day`
```

### oil_gas_midstream/spend_intelligence — BAD

**Q:** Which suppliers have the highest total transaction amount?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT supplier_id, MEASURE(total_spend_usd) AS total_transaction_amount
FROM logistics_demos_catalog.og_midstream_spend.procurement_transactions_metrics
GROUP BY ALL
ORDER BY total_transaction_amount DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `supplier_name`, SUM(`amount_usd`) AS `total_transaction_amount`
FROM `logistics_demos_catalog`.`og_midstream_spend`.`procurement_transactions`
WHERE `supplier_name` IS NOT NULL AND `amount_usd` IS NOT NULL
GROUP BY `supplier_name`
ORDER BY `total_transaction_amount` DESC
LIMIT 10
```

### oil_gas_refining/working_capital_cash_flow_optimization — BAD

**Q:** Which segments have the highest total free cash flow?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
WITH ranked_segments AS ( SELECT segment, SUM(free_cash_flow_usd) AS total_free_cash_flow_usd, RANK() OVER (ORDER BY SUM(free_cash_flow_usd) DESC) AS rnk
FROM logistics_demos_catalog.og_refining_cashflow.cashflow_monthly
WHERE segment IS NOT NULL
GROUP BY segment ) SELECT segment, total_free_cash_flow_usd
FROM ranked_segments
WHERE rnk = 1
```

**Generated SQL:**
```sql
SELECT `segment`, SUM(`free_cash_flow_usd`) AS `total_free_cash_flow_usd`
FROM `logistics_demos_catalog`.`og_refining_cashflow`.`cashflow_monthly`
WHERE `segment` IS NOT NULL AND `free_cash_flow_usd` IS NOT NULL
GROUP BY `segment`
ORDER BY `total_free_cash_flow_usd` DESC
```

### oil_gas_refining/working_capital_cash_flow_optimization — BAD

**Q:** Which business units have the most cash flow amount?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_MISSING_OR_INCORRECT_FILTER

**Expected SQL:**
```sql
WITH ranked_units AS ( SELECT business_unit_name, SUM(amount_usd) AS total_amount_usd, RANK() OVER (ORDER BY SUM(amount_usd) DESC) AS cashflow_rank
FROM logistics_demos_catalog.og_refining_cashflow.cash_transactions
WHERE business_unit_name IS NOT NULL
GROUP BY business_unit_name ) SELECT business_unit_name, total_amount_usd
FROM ranked_units
WHERE cashflow_rank = 1
ORDER BY business_unit_name
```

**Generated SQL:**
```sql
WITH ranked_units AS (
  SELECT 
    `business_unit_name`, 
    SUM(`amount_usd`) AS `total_amount_usd`,
    RANK() OVER (ORDER BY SUM(`amount_usd`) DESC) AS `cash_flow_rank`
  FROM `logistics_demos_catalog`.`og_refining_cashflow`.`cash_transactions`
  WHERE `business_unit_name` IS NOT NULL
  GROUP BY `business_unit_name`
)
SELECT `business_unit_name`, `total_amount_usd`
FROM ranked_units
ORDER BY `cash_flow_rank`
```

### oil_gas_refining/energy_use_monitoring_heat — BAD

**Q:** Which equipment categorys have the highest total solomon energy efficiency index?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_METRIC_CALCULATION

**Expected SQL:**
```sql
SELECT equipment_category, SUM(energy_efficiency_index) AS total_metric
FROM logistics_demos_catalog.og_refining_energy.energy_kpi_monthly
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT `equipment_category`, AVG(`energy_efficiency_index`) AS `avg_energy_efficiency_index`
FROM `logistics_demos_catalog`.`og_refining_energy`.`energy_kpi_monthly`
WHERE `equipment_category` IS NOT NULL AND `energy_efficiency_index` IS NOT NULL
GROUP BY `equipment_category`
ORDER BY `avg_energy_efficiency_index` DESC
```

### oil_gas_refining/energy_use_monitoring_heat — BAD

**Q:** Which heat exchangers have the highest total energy consumption in mmbtu?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE

**Expected SQL:**
```sql
SELECT exchanger_name, SUM(energy_consumption_mmbtu) AS total_energy_consumption_mmbtu
FROM logistics_demos_catalog.og_refining_energy.energy_readings
GROUP BY exchanger_name
ORDER BY total_energy_consumption_mmbtu DESC
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_exchangers AS (
  SELECT 
    `exchanger_id`, 
    SUM(`energy_consumption_mmbtu`) AS `total_energy_consumption_mmbtu`,
    RANK() OVER (ORDER BY SUM(`energy_consumption_mmbtu`) DESC) AS `rank`
  FROM `logistics_demos_catalog`.`og_refining_energy`.`energy_readings`
  WHERE `exchanger_id` IS NOT NULL
  GROUP BY `exchanger_id`
)
SELECT `exchanger_id`, `total_energy_consumption_mmbtu`
FROM ranked_exchangers
WHERE `rank` <= 10
ORDER BY `total_energy_consumption_mmbtu` DESC
```

### oil_gas_refining/financial_analytics_reporting — BAD

**Q:** Which cost centers have the highest total actual revenue?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_MISSING_OR_INCORRECT_FILTER

**Expected SQL:**
```sql
WITH ranked_cost_centers AS ( SELECT cost_center_id, MEASURE(total_actual_revenue) AS total_actual_revenue, RANK() OVER (ORDER BY MEASURE(total_actual_revenue) DESC) AS revenue_rank
FROM logistics_demos_catalog.og_refining_finance.budget_snapshots_metrics
GROUP BY ALL ) SELECT cost_center_id, total_actual_revenue
FROM ranked_cost_centers
WHERE revenue_rank = 1
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_cost_centers AS (
  SELECT 
    `cost_center_id`, 
    MEASURE(`total_actual_revenue`) AS `total_actual_revenue`,
    RANK() OVER (ORDER BY MEASURE(`total_actual_revenue`) DESC) AS rnk
  FROM logistics_demos_catalog.og_refining_finance.budget_snapshots_metrics
  WHERE `cost_center_id` IS NOT NULL
  GROUP BY ALL
)
SELECT `cost_center_id`, `total_actual_revenue`
FROM ranked_cost_centers
WHERE rnk <= 10
ORDER BY `total_actual_revenue` DESC
```

### oil_gas_refining/financial_analytics_reporting — BAD

**Q:** Which cost centers have the highest total transaction amount?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
WITH ranked_cost_centers AS ( SELECT cost_center_id, MEASURE(total_amount_usd) AS total_transaction_amount, RANK() OVER (ORDER BY MEASURE(total_amount_usd) DESC) AS rank
FROM logistics_demos_catalog.og_refining_finance.financial_transactions_metrics
WHERE cost_center_id IS NOT NULL
GROUP BY ALL ) SELECT cost_center_id, total_transaction_amount
FROM ranked_cost_centers
WHERE rank <= 10
ORDER BY total_transaction_amount DESC
```

**Generated SQL:**
```sql
WITH ranked_cost_centers AS (
  SELECT 
    `cost_center_id`, 
    MEASURE(`total_amount_usd`) AS `total_amount_usd`,
    RANK() OVER (ORDER BY MEASURE(`total_amount_usd`) DESC) AS `rank`
  FROM logistics_demos_catalog.og_refining_finance.financial_transactions_metrics
  WHERE `cost_center_id` IS NOT NULL
  GROUP BY ALL
)
SELECT `cost_center_id`, `total_amount_usd`
FROM ranked_cost_centers
WHERE `rank` = 1
```

### oil_gas_integrated/production_monitoring_control_center — BAD

**Q:** Which host platforms have the highest total monthly oil production in barrels?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT platform_name, SUM(total_oil_bbl) AS total_metric
FROM logistics_demos_catalog.og_integrated_production.production_kpi_monthly
GROUP BY 1
ORDER BY total_metric DESC
LIMIT 10
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `snapshot_month`) AS `month`, `platform_name`, SUM(`total_oil_bbl`) AS `total_monthly_oil_bbl`
FROM `logistics_demos_catalog`.`og_integrated_production`.`production_kpi_monthly`
WHERE `platform_name` IS NOT NULL AND `total_oil_bbl` IS NOT NULL
GROUP BY DATE_TRUNC('month', `snapshot_month`), `platform_name`
ORDER BY `total_monthly_oil_bbl` DESC, `month`, `platform_name`
```

### oil_gas_integrated/production_monitoring_control_center — BAD

**Q:** Which wells have the highest total daily oil production in barrels?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_FILTER

**Expected SQL:**
```sql
WITH ranked_wells AS ( SELECT well_id, MEASURE(total_oil_bbl) AS total_oil_bbl, RANK() OVER (ORDER BY MEASURE(total_oil_bbl) DESC) AS rnk
FROM logistics_demos_catalog.og_integrated_production.production_readings_metrics
GROUP BY well_id ) SELECT well_id, total_oil_bbl
FROM ranked_wells
WHERE rnk <= 10
ORDER BY total_oil_bbl DESC
```

**Generated SQL:**
```sql
WITH ranked_wells AS (
  SELECT 
    `well_id`, 
    MEASURE(`total_oil_bbl`) AS `total_oil_bbl`,
    RANK() OVER (ORDER BY MEASURE(`total_oil_bbl`) DESC) AS `rank`
  FROM logistics_demos_catalog.og_integrated_production.production_readings_metrics
  WHERE `status` = 'Flowing'
  GROUP BY ALL
)
SELECT `well_id`, `total_oil_bbl`
FROM ranked_wells
WHERE `rank` <= 10
ORDER BY `total_oil_bbl` DESC
```

### oil_gas_integrated/capital_investment_simulation — BAD

**Q:** How has highest amount changed over time?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION

**Expected SQL:**
```sql
SELECT transaction_date, MEASURE(max_amount) AS max_amount_usd
FROM logistics_demos_catalog.og_integrated_capinvest.investment_transactions_metrics
GROUP BY ALL
ORDER BY transaction_date
```

**Generated SQL:**
```sql
SELECT DATE_TRUNC('month', `transaction_date`) AS `month`, MEASURE(`max_amount`) AS `max_amount_usd`
FROM logistics_demos_catalog.og_integrated_capinvest.investment_transactions_metrics
GROUP BY ALL
ORDER BY `month`
```

### oil_gas_integrated/financial_analytics_reporting — BAD

**Q:** Which cost centers have the highest total actual revenue?

**Reasons:**
- RESULT_EXTRA_ROWS
- LLM_JUDGE_MISSING_OR_INCORRECT_FILTER

**Expected SQL:**
```sql
WITH ranked_cost_centers AS ( SELECT cost_center_id, MEASURE(total_actual_revenue) AS total_actual_revenue, RANK() OVER (ORDER BY MEASURE(total_actual_revenue) DESC) AS revenue_rank
FROM logistics_demos_catalog.og_integrated_finance.budget_snapshots_metrics
GROUP BY ALL ) SELECT cost_center_id, total_actual_revenue
FROM ranked_cost_centers
WHERE revenue_rank = 1
LIMIT 10
```

**Generated SQL:**
```sql
WITH ranked_cost_centers AS (
  SELECT 
    `cost_center_id`, 
    MEASURE(`total_actual_revenue`) AS `total_actual_revenue`,
    RANK() OVER (ORDER BY MEASURE(`total_actual_revenue`) DESC) AS revenue_rank
  FROM logistics_demos_catalog.og_integrated_finance.budget_snapshots_metrics
  GROUP BY ALL
)
SELECT `cost_center_id`, `total_actual_revenue`
FROM ranked_cost_centers
WHERE revenue_rank <= 10
ORDER BY `total_actual_revenue` DESC
```

### oil_gas_integrated/financial_analytics_reporting — BAD

**Q:** Which cost centers have the highest total transaction amount?

**Reasons:**
- RESULT_MISSING_COLUMNS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
WITH ranked_cost_centers AS ( SELECT cost_center_id, MEASURE(total_amount_usd) AS total_transaction_amount, RANK() OVER (ORDER BY MEASURE(total_amount_usd) DESC) AS rank
FROM logistics_demos_catalog.og_integrated_finance.financial_transactions_metrics
WHERE cost_center_id IS NOT NULL
GROUP BY ALL ) SELECT cost_center_id, total_transaction_amount, rank
FROM ranked_cost_centers
WHERE rank <= 10
ORDER BY rank
```

**Generated SQL:**
```sql
WITH ranked_cost_centers AS (
  SELECT 
    `cost_center_id`, 
    MEASURE(`total_amount_usd`) AS `total_transaction_amount`,
    RANK() OVER (ORDER BY MEASURE(`total_amount_usd`) DESC) AS rnk
  FROM logistics_demos_catalog.og_integrated_finance.financial_transactions_metrics
  WHERE `cost_center_id` IS NOT NULL
  GROUP BY ALL
)
SELECT `cost_center_id`, `total_transaction_amount`
FROM ranked_cost_centers
WHERE rnk <= 10
ORDER BY `total_transaction_amount` DESC
```

### oil_gas_integrated/working_capital_cash_flow_optimization — BAD

**Q:** Which business units have the most cash flow amount?

**Reasons:**
- RESULT_MISSING_ROWS
- LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT

**Expected SQL:**
```sql
SELECT business_unit_name, SUM(amount_usd) AS total_amount_usd
FROM logistics_demos_catalog.og_integrated_cashflow.cash_transactions
WHERE business_unit_name IS NOT NULL
GROUP BY business_unit_name
ORDER BY total_amount_usd DESC
```

**Generated SQL:**
```sql
WITH ranked_units AS (
  SELECT 
    business_unit_name, 
    SUM(amount_usd) AS total_amount_usd,
    RANK() OVER (ORDER BY SUM(amount_usd) DESC) AS cashflow_rank
  FROM logistics_demos_catalog.og_integrated_cashflow.cash_transactions
  WHERE business_unit_name IS NOT NULL
  GROUP BY business_unit_name
)
SELECT business_unit_name, total_amount_usd
FROM ranked_units
WHERE cashflow_rank <= 10
ORDER BY cashflow_rank
```
